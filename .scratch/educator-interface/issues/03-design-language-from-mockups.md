# Design language from the mockups

Type: prototype
Status: open

## Question

How should the `Educator LMS Interface Design/*.dc.html` mockups (built for the first-class theme) translate into FLS's brand tokens and default theme? What is the list of cotton components the panel framework must provide to build them (shell and sidebar, page header, tabs, data table and cells, stat tiles, badges, buttons, empty states, quick-view drawer, modal, forms, bulk-action bar and so on)?

Produce a rough rendering of one or two key screens using brand tokens so the human can react, plus a component inventory with each component's responsibilities. Consult `brand-guidelines` and `fls-dev:frontend-styling`.
