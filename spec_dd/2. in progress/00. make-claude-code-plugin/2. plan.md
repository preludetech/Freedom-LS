# Plan: Extract `.claude/` Configuration into an FLS Claude Code Plugin

## Overview

Create the `fls-claude-plugin/` directory at the project root, move/transform ~50 files from `.claude/` into the plugin structure, fix content issues, create the `init` command, set up wrapper scripts and templates, then clean up the project's `.claude/` directory.

## TDD Approach

Each batch includes writing tests first where applicable. For this task, most "tests" are validation checks (plugin loads, paths resolve, hooks fire once, init is idempotent) rather than pytest tests. Verification is done via shell commands and file inspection after each batch.

---

## Batch 1: Create Plugin Skeleton and Manifest

### Task 1.1: Create the plugin directory structure

Create the `fls-claude-plugin/` directory at the project root with all required subdirectories:

```
fls-claude-plugin/
├── .claude-plugin/
├── commands/
│   ├── concrete/
│   ├── periodic/
│   └── spec_driven_dev/
├── agents/
├── skills/
│   ├── admin-interface/
│   ├── alpine-js/
│   ├── frontend-styling/
│   ├── git-worktree-setup/
│   ├── htmx/
│   ├── icon-usage/
│   │   └── resources/
│   ├── markdown-content/
│   ├── multi-tenant/
│   ├── playwright-tests/
│   ├── request-code-review/
│   ├── template/
│   ├── testing/
│   └── use-playwright/
├── docs/
├── hooks/
├── scripts/
│   └── hooks/
└── templates/
    └── wrapper_scripts/
```

### Task 1.2: Create the plugin manifest

Create `fls-claude-plugin/.claude-plugin/plugin.json`:

```json
{
  "name": "fls",
  "version": "1.0.0",
  "description": "FreedomLS development conventions, skills, commands, agents, hooks, and scripts"
}
```

### Task 1.3: Create `.mcp.json` at plugin root

Copy `.claude/mcp.json` to `fls-claude-plugin/.mcp.json` (same content — Playwright MCP config).

### Task 1.4: Create `.lsp.json` at plugin root

Create `fls-claude-plugin/.lsp.json`:

```json
{
  "python": {
    "command": "uv",
    "args": ["run", "pyright", "--stdio"],
    "extensionToLanguage": {
      ".py": "python"
    }
  }
}
```

Also ensure `pyright` is in `pyproject.toml` dev dependencies. Check if it's already there; if not, run `uv add --dev pyright`.

### Task 1.5: Create plugin README

Create `fls-claude-plugin/README.md` with a brief description of the plugin, what it provides, and how to enable it (run `/fls:init`).

### Verification

- Confirm all directories exist
- Confirm `plugin.json` is valid JSON with required fields

---

## Batch 2: Move and Transform Docs

### Task 2.1: Copy all 9 docs to plugin

Copy every file from `.claude/docs/` to `fls-claude-plugin/resources/`:

- `admin_interface.md`
- `email_templates.md`
- `factory_boy.md`
- `frontend_styling.md`
- `markdown_content.md`
- `multi_tenant.md`
- `playwright-testing.md`
- `templates_and_cotton.md`
- `testing.md`

### Task 2.2: Fix `@` references in `testing.md`

In `fls-claude-plugin/resources/testing.md`, replace `@.claude/docs/factory_boy.md` with `${CLAUDE_PLUGIN_ROOT}/resources/factory_boy.md`.

### Task 2.3: Create `agent_memory_guidelines.md`

Extract the shared "Persistent Agent Memory" boilerplate (~40 lines) from `code-reviewer.md` and `qa-data-helper.md` into `fls-claude-plugin/resources/agent_memory_guidelines.md`.

This doc should contain the generic guidelines (what to save, what not to save, explicit user requests, how to organize MEMORY.md) but NOT contain:
- Agent-specific memory paths (those stay in each agent file)
- Agent-specific instructions about what to track
- Session transcript search instructions (remove entirely — not portable)

The memory directory path will be set in each agent file individually and referenced as "your Persistent Agent Memory directory" in the shared doc.

### Verification

- All 10 docs exist in `fls-claude-plugin/resources/`
- No `@.claude/` references remain in any doc file

---

## Batch 3: Move and Transform Skills

### Task 3.1: Copy and transform skills that reference docs

For each of these skills, copy from `.claude/skills/<name>/` to `fls-claude-plugin/skills/<name>/` and replace all `@.claude/docs/<name>.md` with `${CLAUDE_PLUGIN_ROOT}/resources/<name>.md`:

- `testing/SKILL.md` — references `@.claude/docs/testing.md` and `@.claude/docs/factory_boy.md`
- `multi-tenant/SKILL.md` — references `@.claude/docs/multi_tenant.md`
- `playwright-tests/SKILL.md` — references `@.claude/docs/playwright-testing.md` and `@.claude/docs/testing.md`
- `template/SKILL.md` — references `@.claude/docs/templates_and_cotton.md`
- `markdown-content/SKILL.md` — references `@.claude/docs/markdown_content.md`

### Task 3.2: Copy and transform skills with no doc references

These skills have no `@` references and can be copied as-is:

- `htmx/SKILL.md`
- `icon-usage/` (entire directory including `resources/`)

### Task 3.3: Fix and transform `alpine-js/SKILL.md`

Copy to `fls-claude-plugin/skills/alpine-js/SKILL.md` and fix these content issues:

1. **Rule 3** currently says "One JS file — all registrations go in `freedom_ls/base/static/base/js/alpine-components.js`". Update to: "Register components in the `alpine-components.js` of the app that owns the component. There are multiple alpine-components.js files — one per app that needs client-side interactivity."

2. **Existing Components table** — update to include ALL components with correct file locations:

| Component name | File | Used in | Behaviour |
|---------------|------|---------|-----------|
| `dropdownMenu` | `base/…/alpine-components.js` | `cotton/dropdown-menu.html` | Toggle open/close, click-away, smart positioning |
| `modal` | `base/…/alpine-components.js` | `cotton/modal.html` | Toggle open/close, escape key, backdrop click |
| `message` | `base/…/alpine-components.js` | `partials/messages.html` | Auto-dismiss toasts |
| `sidebarComponent` | `base/…/alpine-components.js` | `_base_interface.html` | Toggle open/close, localStorage, responsive |
| `pictureModal` | `base/…/alpine-components.js` | Picture viewing | Full-screen picture modal |
| `scrollTableLabels` | `base/…/alpine-components.js` | Tables | Scroll-synced table labels |
| `debugBadge` | `base/…/alpine-components.js` | `_base.html` | Collapsible debug badge |
| `coursePart` | `student_interface/…/alpine-components.js` | `course_minimal_toc.html` | Expand/collapse with localStorage |
| `tabContainer` | `panel_framework/…/alpine-components.js` | Panel tabs | Tab switching |

### Task 3.4: Fix and transform `admin-interface/SKILL.md`

Copy to `fls-claude-plugin/skills/admin-interface/SKILL.md`. Currently just a one-line doc reference. Improve:

```markdown
---
name: admin-interface
description: Configure Django admin with Unfold and SiteAwareModelAdmin. Use when creating/modifying admin classes, working with inlines, or when the user mentions admin interface.
allowed-tools: Read, Grep, Glob
---

# Admin Interface

## When to Use

Use this skill when:
- Creating or modifying Django admin classes
- Adding admin inlines for related models
- Configuring Unfold theme customizations
- Working with SiteAwareModelAdmin for multi-tenant admin

## Key Rules

- All admin classes must extend `SiteAwareModelAdmin` (not plain `ModelAdmin`)
- Use Unfold's `ModelAdmin` as the base — it's re-exported via `SiteAwareModelAdmin`
- Register with `@admin.register(Model)` decorator pattern
- Inlines use `SiteAwareTabularInline` or `SiteAwareStackedInline`

For full patterns and examples, see `${CLAUDE_PLUGIN_ROOT}/resources/admin_interface.md`.
```

### Task 3.5: Fix and transform `frontend-styling/SKILL.md`

Copy to `fls-claude-plugin/skills/frontend-styling/SKILL.md`. Currently just a one-line doc reference. Improve:

```markdown
---
name: frontend-styling
description: Frontend styling with Tailwind including applying classes to templates and components, creating UI elements, reviewing markup, modifying component classes, and building Tailwind.
allowed-tools: Read, Grep, Glob
---

# Frontend Styling

## When to Use

Use this skill when:
- Applying Tailwind CSS classes to templates and components
- Creating new UI elements or layouts
- Reviewing or modifying markup and styling
- Building Tailwind CSS (`npm run tailwind_build`)

## Key Rules

- Use Tailwind utility classes exclusively — no custom CSS unless absolutely necessary
- Run `npm run tailwind_build` after adding new Tailwind classes that aren't already in use
- Use the project's color palette and spacing scale consistently
- Mobile-first responsive design: start with mobile, add `md:` and `lg:` breakpoints

For full details and examples, see `${CLAUDE_PLUGIN_ROOT}/resources/frontend_styling.md`.
```

### Task 3.6: Fix and transform `use-playwright/SKILL.md`

Copy to `fls-claude-plugin/skills/use-playwright/SKILL.md`. Changes:

1. Remove hardcoded base URL and credentials
2. Add instruction to read `.claude/fls.md` for credentials and base URL
3. Update login flow to use values from `.claude/fls.md`

Replace the "Connection Details" section with:

```markdown
## Connection Details

Read `.claude/fls.md` for the base URL and login credentials before using this skill.
```

Update the login flow to reference "the credentials from `.claude/fls.md`" instead of hardcoded values.

### Task 3.7: Fix and transform `request-code-review/SKILL.md`

Copy entire `request-code-review/` directory to `fls-claude-plugin/skills/request-code-review/`.

Fix line 103: change `@.claude/skills/requesting-code-review/code-reviewer.md` to `${CLAUDE_PLUGIN_ROOT}/skills/request-code-review/code-reviewer.md` (fixes the "requesting" vs "request" typo too).

### Task 3.8: Transform `git-worktree-setup/SKILL.md`

Copy to `fls-claude-plugin/skills/git-worktree-setup/SKILL.md`. Changes:

1. Remove all hardcoded `/home/sheena/workspace/lms/freedom-ls-worktrees/` paths
2. Generalize to describe the bare-repo + worktree pattern without specific paths:

```markdown
# Git Worktree Structure

The project uses a **bare repository** layout:

- The bare repo is the parent directory of all worktree directories
- Each worktree is a sibling directory (e.g., `main/`, `some-feature/`)
- Each worktree has a `.git` **file** (not directory) pointing to its git dir

To create a new worktree:
```bash
cd .. # navigate to the bare repo (parent of current worktree)
git worktree add <branch-name>
cd <branch-name>
./install_dev.sh
```

(Keep the rest of the file about per-branch databases unchanged.)

### Verification

- All 13 skill directories exist in `fls-claude-plugin/skills/`
- No `@.claude/` references remain in any skill file
- No hardcoded `/home/sheena/` paths in any skill file
- No hardcoded `demodev@email.com` in any skill file (except as example in non-credential contexts)

---

## Batch 4: Move and Transform Commands

### Task 4.1: Copy and transform simple commands

Copy these commands as-is (no changes needed):

- `catchup.md`
- `placeholder_page.md`
- `address_pr_review.md`
- `security-review.md`
- `threat-model.md`
- `tdd_implement.md`
- `rebase_main.md`
- `concrete/README.md`
- `concrete/update_fls.md`
- `periodic/README.md`
- `periodic/dependabot_prs.md`
- `spec_driven_dev/README.md`
- `spec_driven_dev/spec_from_idea.md`
- `spec_driven_dev/improve_idea.md`
- `spec_driven_dev/finish_worktree.md`
- `spec_driven_dev/plan_from_spec.md`

### Task 4.2: Fix and transform `commit.md`

Copy to `fls-claude-plugin/commands/commit.md`. Add a branch safety check as step 0:

```
0. Check you're not on main/master: `git branch --show-current`. If on main/master, warn the user and ask for confirmation before proceeding.
```

Keep the rest unchanged.

### Task 4.3: Fix and transform `make_github_issue.md`

Copy to `fls-claude-plugin/commands/make_github_issue.md`. Currently just "Create a github issue". Replace with:

```markdown
Create a GitHub issue for this repository.

1. Gather context: understand what the issue is about from the user's request and any relevant code
2. Determine a clear, concise title
3. Draft the issue body with:
   - Description of the problem or feature request
   - Steps to reproduce (for bugs)
   - Expected vs actual behavior (for bugs)
   - Any relevant code references
4. Ask the user to confirm the title and body before creating
5. Create the issue: `gh issue create --title "<title>" --body "<body>"`
6. Report the issue URL back to the user
```

### Task 4.4: Fix and transform `spec_driven_dev/do_qa.md`

Copy to `fls-claude-plugin/commands/spec_driven_dev/do_qa.md`. Changes:

1. Replace hardcoded `demodev@email.com` credentials with: "Read `.claude/fls.md` for admin credentials"
2. Renumber "Step 1.5" → sequential numbering (Steps 1 through 9):
   - Step 1: Clean up last QA run
   - Step 2: Kill any previous server
   - Step 3: Find an unused PORT and run dev server
   - Step 4: Check branch badge
   - Step 5: (Optional) Login
   - Step 6: Desktop testing
   - Step 7: Mobile testing
   - Step 8: Tablet testing
   - Step 9: Generate report
   - Step 10: Clean up

### Task 4.5: Fix and transform `spec_driven_dev/start_worktree.md`

Copy to `fls-claude-plugin/commands/spec_driven_dev/start_worktree.md`. Changes:

In Step 4, replace the hardcoded example path `/home/user/project-worktrees/` with a generic description:

```markdown
## Step 4: Create the worktree

ALL PREVIOUS STEPS MUST BE COMPLETE BEFORE YOU DO THIS STEP.

Create the worktree from the **bare repo parent directory** (not from inside an existing worktree).

The bare repo is the parent of all worktree directories. To find it, navigate one directory up from your current worktree:

```bash
cd ..  # from current worktree to bare repo
git worktree add <spec-folder-name>
```

IMPORTANT: Never create a worktree inside an existing worktree. Always `cd` to the bare repo parent first.
```

### Task 4.6: Fix and transform `spec_driven_dev/spec_review.md`

Copy to `fls-claude-plugin/commands/spec_driven_dev/spec_review.md`. Changes:

1. Replace `@.claude/docs/` with `${CLAUDE_PLUGIN_ROOT}/resources/`
2. Add `Edit` to `allowed-tools` (currently only `Read, Glob` but the command calls Edit)

### Task 4.7: Fix and transform `spec_driven_dev/implement_plan.md`

Copy to `fls-claude-plugin/commands/spec_driven_dev/implement_plan.md`. Changes:

Fix typo on line 35: "NO NOT run" → "DO NOT run"

### Verification

- All 16+ command files exist in `fls-claude-plugin/commands/`
- No `@.claude/` references remain
- No hardcoded credentials or absolute paths
- `spec_review.md` has `Edit` in `allowed-tools`

---

## Batch 5: Move and Transform Agents

### Task 5.1: Transform and copy `code-reviewer.md`

Copy to `fls-claude-plugin/agents/code-reviewer.md`. Changes:

1. Replace the entire "Persistent Agent Memory" section (lines 173-215) with:
   - Set the memory path to `.claude/agent-memory/code-reviewer/` (relative, stays project-local)
   - Reference the shared guidelines: `For memory usage guidelines, see ${CLAUDE_PLUGIN_ROOT}/resources/agent_memory_guidelines.md`
   - Keep the agent-specific memory instruction about what to record (code patterns, style conventions, etc.)
2. Remove the "Searching past context" section entirely (the session transcript search with `~/.claude/projects/???` is not portable)
3. Replace `@.claude/agent-memory/code-reviewer/` with `.claude/agent-memory/code-reviewer/`

### Task 5.2: Transform and copy `qa-data-helper.md`

Copy to `fls-claude-plugin/agents/qa-data-helper.md`. Changes:

1. Replace `@.claude/docs/factory_boy.md` (×2) with `${CLAUDE_PLUGIN_ROOT}/resources/factory_boy.md`
2. Replace the entire "Persistent Agent Memory" section with:
   - Set the memory path to `.claude/agent-memory/qa-data-factory/` (relative)
   - Reference shared guidelines: `For memory usage guidelines, see ${CLAUDE_PLUGIN_ROOT}/resources/agent_memory_guidelines.md`
   - Keep the agent-specific instruction about tracking QA data requests
3. Remove the "Searching past context" section entirely (hardcoded `/home/sheena/` paths)
4. Remove the hardcoded paths: `/home/sheena/workspace/lms/freedom-ls-worktrees/main/.claude/agent-memory/qa-data-factory/` and the session transcript path

### Verification

- Both agent files exist in `fls-claude-plugin/agents/`
- No `@.claude/` references
- No `/home/sheena/` paths
- No session transcript search instructions
- Both reference `${CLAUDE_PLUGIN_ROOT}/resources/agent_memory_guidelines.md`

---

## Batch 6: Move Hooks and Scripts

### Task 6.1: Move hook scripts to plugin

Copy from `.claude/hooks/` to `fls-claude-plugin/scripts/hooks/`:

- `ruff_fix.sh`
- `post-edit-bandit.sh`
- `security-guard.sh`

Ensure all three are executable (`chmod +x`).

### Task 6.2: Create `hooks/hooks.json`

Create `fls-claude-plugin/hooks/hooks.json` with the hook configuration from the spec. Use `${CLAUDE_PLUGIN_ROOT}/scripts/hooks/` for hook script paths:

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/ruff_fix.sh"
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/post-edit-bandit.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash(git commit:*)",
        "hooks": [
          {
            "type": "command",
            "command": "uv run ruff check . && uv run mypy . && uv run pytest --tb=short -q"
          }
        ]
      },
      {
        "matcher": "Bash|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/security-guard.sh"
          }
        ]
      }
    ]
  }
}
```

### Task 6.3: Copy base scripts to plugin

Copy the following project-root scripts into `fls-claude-plugin/scripts/`:

- `install_dev.sh`
- `dev_db_init.sh`
- `dev_db_delete.sh`
- `db_clear.sh`
- `db_recreate.sh`
- `find_available_port.sh`
- `fetch_pr_comments.sh`
- `kill_runserver.sh`

Also copy `compress_screenshots.py` (from project root) to `fls-claude-plugin/scripts/compress_screenshots.py`. Make sure this is referenced in do_qa

Ensure all `.sh` files are executable.

### Verification

- All scripts exist in `fls-claude-plugin/scripts/` and `fls-claude-plugin/scripts/hooks/`
- All `.sh` files are executable
- `hooks.json` is valid JSON

---

## Batch 7: Create Templates

### Task 7.1: Create `templates/settings.json`

Create `fls-claude-plugin/templates/settings.json` with recommended permissions based on the current `.claude/settings.json` (the `permissions` section only — no `hooks`, no `enabledPlugins`):

```json
{
  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Bash(git status*)",
      "Bash(git diff*)",
      "Bash(git log*)",
      "Bash(find:*)",
      "Bash(uv run pytest:*)",
      "Bash(uv run git commit:*)",
      "mcp__playwright__*",
      "mcp__ide__getDiagnostics",
      "Bash(tail:*)",
      "Bash(git add:*)",
      "Bash(ls:*)",
      "Bash(npm run tailwind_build)",
      "Bash(sleep 3:*)",
      "Bash(sleep 3 && curl -s -o /dev/null -w \"%{http_code}\" http://127.0.0.1:*)",
      "Skill(frontend-styling)",
      "Skill(template)",
      "Skill(icon-usage)"
    ],
    "deny": [
      "Read(.env)",
      "Read(.env.*)",
      "Write(config/settings_prod.py)",
      "Bash(git commit --no-verify:*)",
      "Bash(git push --force:*)",
      "Read(**/*.pem)",
      "Read(**/*.key)"
    ]
  }
}
```

Note: Consolidate the duplicate `mcp__playwright__*` entries from the current settings into the single wildcard.

### Task 7.2: Create `templates/fls.md`

Create `fls-claude-plugin/templates/fls.md`:

```markdown
# FLS Plugin Configuration

## Dev Credentials
- Admin email: demodev@email.com
- Admin password: demodev@email.com
```

### Task 7.3: Create `templates/fls.local.md`

Create `fls-claude-plugin/templates/fls.local.md`:

```markdown
# FLS Local Overrides

<!-- Machine-specific overrides go here. This file is gitignored. -->
<!-- Override any values from fls.md for your local environment. -->
```

### Task 7.4: Create wrapper script templates

Create wrapper scripts in `fls-claude-plugin/templates/wrapper_scripts/`. Each wrapper follows this pattern:

```bash
#!/bin/bash
# <script_name>.sh — Generated by fls plugin init
# Customize the "Project-specific setup" section below.

# FLS_PATH is set during `fls:init` to the path where FLS is installed
# (e.g., "submodules/Freedom-LS" for concrete implementations, or "." for FLS itself)
FLS_PATH="__FLS_PATH__"
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
PLUGIN_DIR="$SCRIPT_DIR/$FLS_PATH/fls-claude-plugin"

# === FLS base setup ===
"$PLUGIN_DIR/scripts/<script_name>.sh" "$@"

# === Project-specific setup ===
# Add your implementation-specific steps below:

```

The `__FLS_PATH__` placeholder is replaced by the `init` command with the actual relative path to FLS from the project root. For example:
- In FLS itself: `FLS_PATH="."` (plugin is at `./fls-claude-plugin/`)
- In a concrete implementation: `FLS_PATH="submodules/Freedom-LS"` (plugin is at `./submodules/Freedom-LS/fls-claude-plugin/`)

Create wrapper templates for:
- `install_dev.sh`
- `dev_db_init.sh`
- `dev_db_delete.sh`
- `db_clear.sh`
- `db_recreate.sh`
- `find_available_port.sh`
- `fetch_pr_comments.sh`
- `kill_runserver.sh`

All must be executable.

### Verification

- All template files exist
- Wrapper scripts are executable
- `settings.json` is valid JSON

---

## Batch 8: Create the `init` Command

### Task 8.1: Create `commands/init.md`

Create `fls-claude-plugin/commands/init.md`. This is a new command that bootstraps the plugin for a project.

```markdown
---
description: Initialise FLS plugin for a project
allowed-tools: Read, Write, Edit, Bash, Glob
---

# Initialize FLS Plugin

Set up the FLS Claude Code plugin for this project.

## Step 1: Merge recommended permissions into `.claude/settings.json`

1. Read `${CLAUDE_PLUGIN_ROOT}/templates/settings.json` for recommended permissions
2. If `.claude/settings.json` exists:
   - Read it and parse the existing permissions
   - Add any missing `allow` rules (don't duplicate existing ones)
   - Add any missing `deny` rules (don't duplicate existing ones)
   - Add `"fls": true` to `enabledPlugins` (create the key if it doesn't exist)
   - Remove the `hooks` section if present (plugin now owns hooks)
   - Write the updated file
3. If `.claude/settings.json` doesn't exist:
   - Create it from the template with `enabledPlugins: {"fls": true}` added
4. Report what was added/changed

## Step 2: Create `.claude/fls.md`

1. If `.claude/fls.md` already exists, skip this step
2. Ask the user for:
   - Dev admin email (default: `demodev@email.com`)
   - Dev admin password (default: `demodev@email.com`)
   - Base URL (default: `http://127.0.0.1:8000`)
3. Generate `.claude/fls.md` from `${CLAUDE_PLUGIN_ROOT}/templates/fls.md`, substituting the user's values

## Step 3: Create `.claude/fls.local.md`

1. If `.claude/fls.local.md` already exists, skip this step
2. Copy from `${CLAUDE_PLUGIN_ROOT}/templates/fls.local.md`

## Step 4: Update `.gitignore`

1. Read `.gitignore`
2. If `.claude/fls.local.md` is not already listed, add it
3. If `.claude/settings.local.json` is not already listed, check and add if missing

## Step 5: Determine FLS path

1. Ask the user for the relative path from the project root to FLS (e.g., `submodules/Freedom-LS`)
   - Default: `.` (for FLS itself, where the plugin is at `./fls-claude-plugin/`)
   - For concrete implementations, this is typically `submodules/Freedom-LS`
2. Validate that `<fls_path>/fls-claude-plugin/` exists
3. Store this path for use in wrapper script generation

## Step 6: Generate wrapper scripts at project root

For each wrapper script template in `${CLAUDE_PLUGIN_ROOT}/templates/wrapper_scripts/`:
1. Check if a script with that name already exists at the project root
2. If it exists, **skip it** (never overwrite existing scripts)
3. If it doesn't exist, copy the template to the project root, replace `__FLS_PATH__` with the path from Step 5, and make it executable

## Step 7: Validate the setup

Run these checks and report results:

1. Confirm `fls` is in `enabledPlugins` in `.claude/settings.json`
2. Confirm `.claude/fls.md` exists and contains required fields (email, password, base URL)
3. Confirm wrapper scripts exist at project root and are executable
4. Confirm hook scripts in the plugin (`scripts/hooks/*.sh`) are executable
5. Confirm no `hooks` section in `.claude/settings.json`
6. Confirm wrapper scripts have the correct `FLS_PATH` (not `__FLS_PATH__`)
7. Report any issues found

Print a summary of everything that was done.
```

### Verification

- `init.md` exists in `fls-claude-plugin/commands/`
- The command references `${CLAUDE_PLUGIN_ROOT}` for all plugin-internal paths

---

## Batch 9: Delete Stale Files

### Task 9.1: Delete stale brand-guidelines files

Delete from `.claude/skills/brand-guidelines/`:
- `oldSKILL.md`
- `todo`

Keep `SKILL.md` — it stays project-local.

### Task 9.2: Delete `do_todos.md`

Delete `.claude/commands/do_todos.md` — no longer needed per spec.

### Verification

- `oldSKILL.md`, `todo`, and `do_todos.md` no longer exist

---

## Batch 10: Clean Up Project `.claude/` Directory

### Task 10.1: Remove files that moved to plugin

Remove from `.claude/` all files that now live in the plugin:

**Commands** (entire directories/files):
- `.claude/commands/rebase_main.md`
- `.claude/commands/tdd_implement.md`
- `.claude/commands/address_pr_review.md`
- `.claude/commands/security-review.md`
- `.claude/commands/threat-model.md`
- `.claude/commands/commit.md`
- `.claude/commands/make_github_issue.md`
- `.claude/commands/catchup.md`
- `.claude/commands/placeholder_page.md`
- `.claude/commands/concrete/` (entire directory)
- `.claude/commands/periodic/` (entire directory)
- `.claude/commands/spec_driven_dev/` (entire directory)

**Skills** (all except `brand-guidelines`):
- `.claude/skills/admin-interface/`
- `.claude/skills/alpine-js/`
- `.claude/skills/frontend-styling/`
- `.claude/skills/git-worktree-setup/`
- `.claude/skills/htmx/`
- `.claude/skills/icon-usage/`
- `.claude/skills/markdown-content/`
- `.claude/skills/multi-tenant/`
- `.claude/skills/playwright-tests/`
- `.claude/skills/request-code-review/`
- `.claude/skills/template/`
- `.claude/skills/testing/`
- `.claude/skills/use-playwright/`

**Agents:**
- `.claude/agents/code-reviewer.md`
- `.claude/agents/qa-data-helper.md`

**Docs** (entire directory):
- `.claude/docs/`

**Hooks** (entire directory):
- `.claude/hooks/`

**MCP config:**
- `.claude/mcp.json`

### Task 10.2: Update `.claude/settings.json`

Remove the `hooks` section from `.claude/settings.json`. Add `"fls": true` to `enabledPlugins`. Keep the `permissions` section unchanged.

The resulting file should look like:

```json
{
  "permissions": {
    "allow": [ ... existing rules ... ],
    "deny": [ ... existing rules ... ]
  },
  "enabledPlugins": {
    "fls": true
  }
}
```

### Task 10.3: Create `.claude/fls.md` for this project

Create `.claude/fls.md` with the project's actual dev credentials:

```markdown
# FLS Plugin Configuration

## Dev Credentials
- Admin email: demodev@email.com
- Admin password: demodev@email.com
```

### Task 10.4: Create `.claude/fls.local.md`

Create `.claude/fls.local.md` (empty template for local overrides).

### Task 10.5: Update `.gitignore`

Add `.claude/fls.local.md` to `.gitignore` if not already present.

### Task 10.6: Replace project root scripts with wrappers

The project root currently has the base scripts directly. Replace them with wrapper scripts that call the plugin's base scripts.

Since this is FLS itself (not a concrete implementation), `FLS_PATH="."` — the plugin is at `./fls-claude-plugin/`.

For each of: `install_dev.sh`, `dev_db_init.sh`, `dev_db_delete.sh`, `db_clear.sh`, `db_recreate.sh`, `find_available_port.sh`, `fetch_pr_comments.sh`, `kill_runserver.sh`:

1. The plugin's `scripts/` directory already has the base version (copied in Batch 6)
2. Replace the project root script with a wrapper that sets `FLS_PATH="."`, calls the plugin base script, and preserves any project-specific logic

For `install_dev.sh` specifically, the wrapper needs to preserve the project-specific steps (Playwright MCP setup, demo data loading) while delegating the base steps to the plugin script. Review the current script and split accordingly.

### Verification

- `.claude/` directory contains only: `agent-memory/`, `skills/brand-guidelines/`, `fls.md`, `fls.local.md`, `settings.json`, `settings.local.json`
- No duplicate files between plugin and `.claude/`
- `.claude/settings.json` has no `hooks` section
- `.claude/settings.json` has `enabledPlugins.fls: true`
- `.gitignore` includes `.claude/fls.local.md`

---

## Batch 11: Add Pyright Dev Dependency

### Task 11.1: Add pyright to dev dependencies

Check `pyproject.toml` for existing pyright dependency. If not present:

```bash
uv add --dev pyright
```

### Verification

- `pyright` appears in `[dependency-groups]` dev section of `pyproject.toml`
- `uv run pyright --version` works

---

## Batch 12: Final Validation

### Task 12.1: Verify plugin loads

Run:
```bash
claude --plugin-dir ./fls-claude-plugin --print-plugins
```
or equivalent to verify the plugin loads all skills, commands, agents, hooks, MCP, and LSP config.

### Task 12.2: Verify no broken references

```bash
# Check for any remaining @.claude/ references in plugin
grep -r '@\.claude/' fls-claude-plugin/ || echo "No @.claude/ references found"

# Check for any hardcoded /home/sheena/ paths in plugin
grep -r '/home/sheena/' fls-claude-plugin/ || echo "No hardcoded paths found"

# Check for hardcoded demodev credentials in plugin (except templates)
grep -r 'demodev@email.com' fls-claude-plugin/ --include='*.md' | grep -v templates/ || echo "No hardcoded credentials in non-template files"
```

### Task 12.3: Run tests

```bash
uv run pytest
```

All existing tests must still pass.

### Task 12.4: Verify hooks don't duplicate

Confirm that `.claude/settings.json` has no `hooks` section and that `fls-claude-plugin/hooks/hooks.json` is the sole hook source.

---

## Skills and MCPs to Use

| Step | Skill/MCP | Why |
|------|-----------|-----|
| All template/HTML work | `template` skill | Follows cotton component and template conventions |
| Testing/verification | `testing` skill | TDD patterns and pytest conventions |
| Code review after each batch | `request-code-review` skill | Catch issues between batches |
| Git commits | `commit` command | Follows project commit conventions |
| Any git worktree operations | `git-worktree-setup` skill | Understands the bare repo layout |

---

## Success Criteria Checklist

1. [ ] Plugin loads correctly with all skills, commands, agents, hooks, MCP, LSP
2. [ ] No broken references (`${CLAUDE_PLUGIN_ROOT}` resolves, no `@` refs to plugin internals, no hardcoded paths)
3. [ ] `init` command works end-to-end on a fresh project
4. [ ] `init` is idempotent (running twice doesn't duplicate or overwrite)
5. [ ] Hooks run once (plugin owns hooks, project settings.json has none)
6. [ ] Credentials are configurable via `.claude/fls.md`
7. [ ] Scripts are extensible via wrapper pattern
8. [ ] Existing project still works identically after extraction
9. [ ] `/reload-plugins` picks up changes during development
10. [ ] `init` validation passes on correctly configured project
11. [ ] Pyright LSP starts and provides diagnostics
12. [ ] All content fixes applied (alpine-js table, typos, spec_review allowed-tools, etc.)
