# Spec: Extract `.claude/` Configuration into an FLS Claude Code Plugin

## Overview

Turn the current `.claude/` directory contents into a reusable Claude Code plugin called `fls`. The plugin lives inside the FreedomLS repository. Concrete FreedomLS implementations install the plugin and get all FLS development conventions, skills, commands, agents, hooks, and scripts out of the box.

## Why This Matters

The `.claude/` directory contains 60 files (14 skills, 15 commands, 2 agents, 9 docs, 3 hooks, shell scripts, MCP config) that define FreedomLS development conventions. These are generic to any FreedomLS implementation but currently only exist in this repo. Extracting them into a plugin makes them:

- **Portable**: Any FreedomLS implementation gets the full development experience by enabling one plugin
- **Maintainable**: Conventions are updated in one place (the plugin) rather than copied across implementations
- **Clean**: The project's `.claude/` directory becomes minimal — just project-specific config and plugin enablement

---

## Plugin Structure

```
fls-claude-plugin/
├── .claude-plugin/
│   └── plugin.json              # Plugin manifest: name, version, userConfig
├── commands/
│   ├── address_pr_review.md
│   ├── catchup.md
│   ├── commit.md
│   ├── init.md                  # NEW: Initialise plugin for a project
│   ├── make_github_issue.md
│   ├── placeholder_page.md
│   ├── rebase_main.md
│   ├── security-review.md
│   ├── tdd_implement.md
│   ├── threat-model.md
│   ├── concrete/
│   │   ├── README.md
│   │   └── update_fls.md
│   ├── periodic/
│   │   ├── README.md
│   │   └── dependabot_prs.md
│   └── spec_driven_dev/
│       ├── README.md
│       ├── do_qa.md
│       ├── finish_worktree.md
│       ├── implement_plan.md
│       ├── improve_idea.md
│       ├── plan_from_spec.md
│       ├── spec_from_idea.md
│       ├── spec_review.md
│       └── start_worktree.md
├── agents/
│   ├── code-reviewer.md
│   └── qa-data-helper.md
├── skills/
│   ├── admin-interface/SKILL.md
│   ├── alpine-js/SKILL.md
│   ├── frontend-styling/SKILL.md
│   ├── git-worktree-setup/SKILL.md
│   ├── htmx/SKILL.md
│   ├── icon-usage/
│   │   ├── SKILL.md
│   │   └── resources/
│   │       ├── configuring-icons.md
│   │       └── custom-icon-backend.md
│   ├── markdown-content/SKILL.md
│   ├── multi-tenant/SKILL.md
│   ├── playwright-tests/SKILL.md
│   ├── request-code-review/
│   │   ├── SKILL.md
│   │   └── code-reviewer.md
│   ├── template/SKILL.md
│   ├── testing/SKILL.md
│   └── use-playwright/SKILL.md
├── docs/
│   ├── admin_interface.md
│   ├── email_templates.md
│   ├── factory_boy.md
│   ├── frontend_styling.md
│   ├── markdown_content.md
│   ├── multi_tenant.md
│   ├── playwright-testing.md
│   ├── templates_and_cotton.md
│   └── testing.md
├── hooks/
│   └── hooks.json               # Hook event configuration
├── scripts/
│   ├── hooks/
│   │   ├── ruff_fix.sh
│   │   ├── post-edit-bandit.sh
│   │   └── security-guard.sh
│   ├── dev_db_init.sh
│   ├── dev_db_delete.sh
│   ├── db_clear.sh
│   ├── db_recreate.sh
│   ├── install_dev.sh
│   ├── find_available_port.sh
│   ├── fetch_pr_comments.sh
│   ├── kill_runserver.sh
│   └── compress_screenshots.py
├── templates/
│   ├── settings.json            # Recommended permissions template
│   ├── fls.md                   # Template for project plugin config
│   ├── fls.local.md             # Empty template for local overrides
│   └── wrapper_scripts/
│       ├── install_dev.sh
│       ├── dev_db_init.sh
│       ├── dev_db_delete.sh
│       ├── db_clear.sh
│       ├── db_recreate.sh
│       ├── find_available_port.sh
│       ├── fetch_pr_comments.sh
│       └── kill_runserver.sh
├── .mcp.json                    # Playwright MCP config
├── .lsp.json                    # Python LSP (Pyright) config
└── README.md
```

---

## What Gets Extracted vs What Stays Project-Local

### Moves to Plugin (50+ files)

| Category | Files | Notes |
|----------|-------|-------|
| Skills | 13 generic skills | All except `brand-guidelines` |
| Commands | 15 commands + 1 new (`init`) | All existing commands including `rebase_main`, `concrete/`, and `periodic/` (excludes deleted `do_todos.md`) |
| Agents | `code-reviewer`, `qa-data-helper` | Agent definitions only, not memory |
| Docs | All 9 reference docs | Moved from `.claude/docs/` to plugin `docs/` |
| Hooks | `ruff_fix.sh`, `post-edit-bandit.sh`, `security-guard.sh` | Plus `hooks.json` configuration |
| Scripts | `install_dev.sh`, `dev_db_*.sh`, `find_available_port.sh`, `fetch_pr_comments.sh`, `kill_runserver.sh`, `db_clear.sh`, `db_recreate.sh`, `compress_screenshots.py` | Base versions; projects get wrappers. `compress_screenshots.py` currently lives at the project root (not in `.claude/`) and supports spec-driven dev workflow (no wrapper needed). |
| MCP | Playwright config | `.mcp.json` at plugin root |
| LSP | Python (Pyright) config | `.lsp.json` at plugin root |

### Stays Project-Local

| Item | Reason |
|------|--------|
| `.claude/settings.json` | Permissions can't be shipped via plugin `settings.json` (only `agent` key supported). Contains allow/deny rules + `enabledPlugins`. |
| `.claude/settings.local.json` | User-specific overrides, gitignored |
| `.claude/fls.md` | Project-scoped plugin config (dev credentials, project-specific settings). Committed to git. |
| `.claude/fls.local.md` | Machine-specific overrides, gitignored |
| `.claude/agent-memory/` | Runtime state accumulated per-project |
| `brand-guidelines` skill | Branding is implementation-specific |
| `docker-entrypoint.sh` | Deployment-specific, not a dev tool |

### Deleted During Extraction

| Item | Reason |
|------|--------|
| `brand-guidelines/oldSKILL.md` | Stale, references outdated icon libraries |
| `brand-guidelines/todo` | Associated with stale file |
| `do_todos.md` | No longer needed — removed |

---

## Key Design Decisions

### 1. No `@` Path References Inside the Plugin

**Decision**: Plugin files must not use `@` syntax for referencing other plugin files.

**Why**: In plugins, `@` resolves to the **project root**, not the plugin root. Different FreedomLS implementations have different project roots, so `@` references to plugin-internal files would break.

**How it works**: All internal references use `${CLAUDE_PLUGIN_ROOT}/path/to/file`. For example, the testing skill references docs via `${CLAUDE_PLUGIN_ROOT}/docs/factory_boy.md` instead of `@.claude/docs/factory_boy.md`.

### 2. `./` Paths for Project Root Scripts

**Decision**: Plugin commands/skills that invoke developer-facing scripts in shell commands use `./script_name.sh` to reference the project root wrapper scripts.

**Why**: Wrapper scripts at the project root contain project-specific customizations. The plugin must call the wrappers (not its own base scripts directly) so that all custom code is executed. Since Claude Code always runs shell commands from the project root (CWD), `./` correctly resolves to the project root wrappers. Note: `@` is a Claude Code context directive and does not work inside bash commands.

### 3. No Hardcoded Absolute Paths

**Decision**: All paths within the plugin use `${CLAUDE_PLUGIN_ROOT}` or relative paths. No `/home/sheena/...` or similar.

**Why**: The plugin must work on any machine, in any FreedomLS implementation.

**Files requiring changes**:
- `git-worktree-setup/SKILL.md` — currently hardcodes `/home/sheena/workspace/lms/freedom-ls-worktrees/`. Generalize to describe the bare-repo + worktree pattern without specific paths.
- `qa-data-helper.md` — currently hardcodes absolute paths for agent memory search and session transcript paths. Use relative paths (`.claude/agent-memory/qa-data-factory/`) and remove session transcript references (those are user-specific).

### 4. Permissions Stay Project-Local (with Template)

**Decision**: The plugin does NOT ship permissions. The project's `.claude/settings.json` carries all allow/deny rules.

**Why**: Claude Code plugin `settings.json` currently only supports the `agent` key — not `permissions`. This is a platform limitation.

**Mitigation**: The plugin includes a `templates/settings.json` with recommended permissions. The `init` command merges these into the project's settings.json (adding to existing rules, never overwriting).

### 5. Hook Ownership Moves to Plugin

**Decision**: All hook definitions (PostToolUse, PreToolUse) move to the plugin's `hooks/hooks.json`. The project's `settings.json` removes its `hooks` section.

**Why**: If both define hooks, they run twice. The plugin is the single owner of FLS development hooks.

**hooks/hooks.json contents**:
```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/ruff_fix.sh"
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/post-edit-bandit.sh"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash(git commit:*)",
        "hooks": [
          {
            "type": "command",
            "command": "uv run ruff check . && uv run mypy . && uv run pytest --tb=short -q"
          }
        ]
      },
      {
        "matcher": "Bash|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/hooks/security-guard.sh"
          }
        ]
      }
    ]
  }
}
```

### 6. Credential Configuration via `.claude/fls.md`

**Decision**: Dev credentials (e.g., `demodev@email.com`) are configured in `.claude/fls.md`, a project-level file committed to git. Machine-specific overrides go in `.claude/fls.local.md` (gitignored).

**Why**: Dev credentials are project-scoped — all dev machines share the same test users set up by shared dev utilities. Unlike `userConfig` in plugin.json (which is per-user and prompted at enable time), `.claude/fls.md` is shared via version control.

**How skills access it**: Skills/commands that need credentials include an instruction to `Read .claude/fls.md` before proceeding. This adds one tool call per invocation but is explicit and reliable.

**Template `.claude/fls.md`**:
```markdown
# FLS Plugin Configuration

## Dev Credentials
- Admin email: demodev@email.com
- Admin password: demodev@email.com

## Project Settings
- Base URL: http://127.0.0.1:8000
```

### 7. Python LSP via Pyright

**Decision**: The plugin ships a `.lsp.json` configuring Pyright as the Python language server. Pyright is added as a dev dependency (`uv add --dev pyright`).

**Why**: LSP gives Claude Code access to diagnostics (type errors, undefined names, import issues) as it works, catching problems earlier than test runs. Pyright is fast, has strong Django support, and works well with the project's type-hinting conventions.

**`.lsp.json` contents**:
```json
{
  "python": {
    "command": "uv",
    "args": ["run", "pyright", "--stdio"],
    "extensionToLanguage": {
      ".py": "python"
    }
  }
}
```

Using `uv run pyright` ensures the correct virtual environment is used. The `install_dev.sh` base script must include `uv add --dev pyright` (or just ensure it's in `pyproject.toml` dev dependencies so `uv sync` installs it).

### 8. Extensible Script Architecture

**Decision**: The plugin ships base scripts in `scripts/`. The `init` command generates wrapper scripts at the project root. Plugin commands call the project root wrappers (via `./script_name.sh`), not the plugin's base scripts directly.

**Why**: Different FreedomLS implementations need to customize developer scripts. For example:
- `install_dev.sh` might have extra setup steps for a specific implementation
- Demo data loading might differ entirely between implementations
- Some scripts may not be needed at all

**How it works**:

1. **Plugin base scripts** (`fls-claude-plugin/scripts/`) contain the shared FLS logic (DB creation, migrations, base setup)
2. **`init` command** generates wrapper scripts at the project root from templates (`fls-claude-plugin/templates/wrapper_scripts/`)
3. **Wrapper scripts** call the plugin's base script and have a clearly marked section for project-specific additions:

```bash
#!/bin/bash
# install_dev.sh — Generated by fls plugin init
# Customize the "Project-specific setup" section below.

PLUGIN_DIR="$(dirname "$(readlink -f "$0")")/fls-claude-plugin"

# === FLS base setup ===
"$PLUGIN_DIR/scripts/install_dev.sh"

# === Project-specific setup ===
# Add your implementation-specific setup below:

```

4. **Plugin commands** reference `./install_dev.sh` in shell commands (CWD is always the project root), ensuring customizations are always included
5. **`init` command** never overwrites existing wrapper scripts — if a project root script already exists, it's left as-is

---

## The `init` Command

A new command (`commands/init.md`) that bootstraps the plugin for a concrete FreedomLS implementation.

### What It Does

1. **Merges recommended permissions into `.claude/settings.json`**
   - Reads the plugin's `templates/settings.json` for recommended allow/deny rules
   - If `.claude/settings.json` exists, adds missing permissions without overwriting existing ones
   - If it doesn't exist, creates it from the template
   - Adds `"fls": true` to `enabledPlugins`
   - Removes any `hooks` section (plugin now owns hooks)

2. **Creates `.claude/fls.md`**
   - Asks the user for dev credentials (email, password) and base URL
   - Generates the file from the plugin's template
   - If the file already exists, skips creation

3. **Creates `.claude/fls.local.md`**
   - Creates an empty template file for machine-specific overrides
   - If it already exists, skips

4. **Adds `.claude/fls.local.md` to `.gitignore`**
   - Checks if the entry already exists before adding

5. **Generates wrapper scripts at project root**
   - Copies wrapper script templates from `templates/wrapper_scripts/`
   - Never overwrites existing scripts
   - Makes all scripts executable

6. **Validates the setup**
   - Confirms plugin is enabled in `settings.json`
   - Confirms `.claude/fls.md` exists and has required fields
   - Confirms wrapper scripts exist and are executable
   - Confirms hook scripts in the plugin are executable
   - Reports any issues found

### Interactive Flow

```
$ /fls:init

Initializing FLS plugin...

> Dev admin email [demodev@email.com]:
> Dev admin password [demodev@email.com]:
> Base URL [http://127.0.0.1:8000]:

✓ Updated .claude/settings.json (added 30 permissions, enabled fls plugin)
✓ Created .claude/fls.md
✓ Created .claude/fls.local.md
✓ Added .claude/fls.local.md to .gitignore
✓ Generated wrapper scripts: install_dev.sh, dev_db_init.sh, ...
✓ Validation passed

FLS plugin is ready!
```

---

## File Transformation Details

### Files Requiring `@` → `${CLAUDE_PLUGIN_ROOT}` Changes

Every file that currently uses `@.claude/docs/...` or `@.claude/...` to reference plugin-internal files must be updated:

| File | Current Reference | New Reference |
|------|-------------------|---------------|
| `qa-data-helper.md` (agent) | `@.claude/docs/factory_boy.md` (×2) | `${CLAUDE_PLUGIN_ROOT}/docs/factory_boy.md` |
| `code-reviewer.md` (agent) | `@.claude/agent-memory/code-reviewer/` | `.claude/agent-memory/code-reviewer/` (stays project-local, use relative path) |
| `docs/testing.md` | `@.claude/docs/factory_boy.md` | `${CLAUDE_PLUGIN_ROOT}/docs/factory_boy.md` |
| `request-code-review/SKILL.md` | `@.claude/skills/requesting-code-review/code-reviewer.md` | `${CLAUDE_PLUGIN_ROOT}/skills/request-code-review/code-reviewer.md` |
| `spec_driven_dev/spec_review.md` | `@.claude/docs/` | `${CLAUDE_PLUGIN_ROOT}/docs/` |
| Skills referencing docs (`admin-interface`, `playwright-tests`, `frontend-styling`, `template`, `multi-tenant`, `testing`, `markdown-content`) | `@.claude/docs/<name>.md` | `${CLAUDE_PLUGIN_ROOT}/docs/<name>.md` |

### Files with Project Root Script References

Commands that invoke developer scripts in bash commands should keep using `./` (relative to project root, which is always the CWD). No change needed — these already work correctly because the wrapper scripts live at the project root:

| File | Reference | Action |
|------|-----------|--------|
| `spec_driven_dev/do_qa.md` | `./find_available_port.sh` | No change needed (shell command, CWD = project root) |
| `git-worktree-setup/SKILL.md` | `./install_dev.sh` | No change needed (shell command, CWD = project root) |

### Files Requiring Hardcoded Path Removal

| File | Current Path | Replacement |
|------|-------------|-------------|
| `git-worktree-setup/SKILL.md` | `/home/sheena/workspace/lms/freedom-ls-worktrees/` | Generic description of bare-repo + worktree pattern |
| `qa-data-helper.md` | `/home/sheena/workspace/lms/freedom-ls-worktrees/main/.claude/agent-memory/...` | `.claude/agent-memory/qa-data-factory/` (relative) |
| `qa-data-helper.md` | `/home/sheena/.claude/projects/.../*.jsonl` | Remove — session transcripts are user-specific |

### Files Requiring Credential Parameterization

| File | Current Hardcoded Value | New Approach |
|------|------------------------|--------------|
| `spec_driven_dev/do_qa.md` | `demodev@email.com` | Instruction to read `.claude/fls.md` for credentials |
| `use-playwright/SKILL.md` | `demodev@email.com` / password | Instruction to read `.claude/fls.md` for credentials |

---

## Post-Extraction Project `.claude/` Directory

After extraction, the project's `.claude/` directory becomes minimal:

```
.claude/
├── agent-memory/
│   ├── code-reviewer/MEMORY.md
│   └── qa-data-factory/
├── skills/
│   └── brand-guidelines/
│       └── SKILL.md
├── fls.md                    # Plugin config (committed)
├── fls.local.md              # Local overrides (gitignored)
├── settings.json             # Permissions + enabledPlugins (no hooks)
└── settings.local.json       # User-specific (gitignored)
```

The root `CLAUDE.md` remains but can be slimmed down — conventions carried by the plugin's skills and docs no longer need to be duplicated there.

---

## Skill/Command Content Fixes (During Extraction)

These issues exist in the current files and should be fixed as part of the extraction to the plugin.

### Code Inconsistencies

| File | Issue | Fix |
|------|-------|-----|
| `alpine-js/SKILL.md` | States "One JS file — all registrations go in `base/js/alpine-components.js`" but there are actually 3 separate `alpine-components.js` files: `base/`, `student_interface/`, and `panel_framework/` | Update to acknowledge multiple files. The rule should be: "Register components in the `alpine-components.js` of the app that owns the component." |
| `alpine-js/SKILL.md` | "Existing Components" table is incomplete — missing `pictureModal`, `scrollTableLabels` (base), `tabContainer` (panel_framework). Also, `coursePart` is listed as being in the base file but it's actually in `student_interface/` | Update the table to list all components with their correct file locations |
| `request-code-review/SKILL.md` | Line 103 references `@.claude/skills/requesting-code-review/code-reviewer.md` but directory is `request-code-review` (typo: "requesting" vs "request") | Fix path in the reference |
| `code-reviewer.md` (agent) | "Searching past context" section tells the agent to search `path="~/.claude/projects/???"` with literal question marks — this doesn't work as a path | Remove the session transcript search instruction entirely (user-specific, not portable) |
| `spec_driven_dev/spec_review.md` | `allowed-tools` only lists `Read, Glob` but the command calls `Edit` to fix spec files | Add `Edit` to `allowed-tools` |
| `spec_driven_dev/implement_plan.md` | Contains "NO NOT run" (typo for "DO NOT") | Fix typo |

### Prompt Engineering Improvements

| File | Issue | Improvement |
|------|-------|-------------|
| `admin-interface/SKILL.md` | Entire content is just a one-line reference to docs — provides no context for when/how to use it | Add a brief "When to Use" section and key rules summary (like other skills have), keeping the doc reference for details |
| `frontend-styling/SKILL.md` | Same issue — just a one-line doc reference with no summary | Add a brief "When to Use" section and key rules summary |
| `make_github_issue.md` | Entire content is "Create a github issue" — no structure, no instructions | Add steps: gather context, determine title/labels, use `gh issue create`, confirm with user |
| `spec_driven_dev/start_worktree.md` | Step 4 example uses a hardcoded path `/home/user/project-worktrees/` — this won't generalize | Use a description like "Navigate to the parent of the current worktree directory (the bare repo)" instead of showing a specific path |
| `spec_driven_dev/do_qa.md` | "Step 1.5" naming is confusing — suggests it was inserted between existing steps rather than being integral | Renumber steps sequentially (1 through 9) |
| `use-playwright/SKILL.md` | Hardcodes `Base URL: http://127.0.0.1:8000` — but the do_qa command uses dynamic ports, creating an inconsistency | Change to instruction to read base URL from `.claude/fls.md` (aligns with credential parameterization) |
| Agents (both) | Large duplicated blocks of "Persistent Agent Memory" instructions (~40 lines each) are identical boilerplate | Extract the shared memory instructions into a doc file (`docs/agent_memory_guidelines.md`) and reference it from both agents via `${CLAUDE_PLUGIN_ROOT}/docs/agent_memory_guidelines.md` |
| `commit.md` | Missing a step to check for branch safety (not on main) — other commands like `implement_plan.md` have this check | Add a step to verify not on main/master before committing |

---

## Success Criteria

1. **Plugin loads correctly**: Running `claude --plugin-dir ./fls-claude-plugin` loads all skills, commands, agents, hooks, MCP config, and LSP config without errors
2. **No broken references**: All `${CLAUDE_PLUGIN_ROOT}` paths resolve correctly. No `@` references to plugin-internal files. No hardcoded absolute paths.
3. **`init` command works end-to-end**: Running `/fls:init` on a fresh project sets up settings.json, fls.md, fls.local.md, .gitignore, and wrapper scripts correctly
4. **`init` is idempotent**: Running it twice doesn't duplicate permissions, overwrite existing files, or create duplicate .gitignore entries
5. **Hooks run once**: With hooks in the plugin and removed from project settings.json, each hook fires exactly once per event
6. **Credentials are configurable**: Skills that need dev credentials read from `.claude/fls.md` and work with project-specific values
7. **Scripts are extensible**: Wrapper scripts at the project root correctly call plugin base scripts and allow project-specific additions
8. **Existing project still works**: After extraction, the current FreedomLS repo works identically — the plugin is installed and enabled, all functionality preserved
9. **`/reload-plugins`** picks up changes during development without restarting Claude Code
10. **Validation passes**: The `init` command's validation step reports no issues on a correctly configured project
11. **Python LSP works**: Pyright language server starts and provides diagnostics for Python files
12. **Skill/command content is accurate**: All code inconsistencies and prompt engineering issues listed above are resolved
