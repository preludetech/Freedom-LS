First, go over all the QA reports from here `spec_dd/3. done` and identify all the QA errors.

Also make use of the playwright mcp to check that everything else works as it should. Spot any bugs.

If you need any data to be created or edited to get this right, use the qa-data-helper agent to help edit and create whatever is needed.

Once this is done, we need a  spec that describes how to address any problems that were seen.
