Currently, the FLS Claude plugin directory contains a lot of things that are not only useful within the context of this repository. For example, when I am writing any Django application, I would want to follow certain practices around testing, tailwind, HTMX, etc.

create a new Cloud plugin directory that contains reusable skills that are helpful across multiple Django projects. This would include:

- testing
- templates
- cotton
- htmx
- factory boy
- template partials
- django tasks
And any non-fls, stack specific setup. This should still be useful for fls
