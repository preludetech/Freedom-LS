Courses can have thumbnail images.

When we make use of the management script freedom_ls/content_engine/management/commands/content_save.py then, if a thumbnail is defined, save it in the database.

Not all courses will have thumbnail images.

Update one of the demo_content courses to demonstrate this functionality.

Update the course card component so it shows the thumbnail image if there is one.
