# Background

We recently implemented `spec_dd/3. done/2026-03-12_09:95_worktree-dx/1. spec.md `

In the database we have the following sites set up

```
name: DemoDev
domain: 127.0.0.1:8000
admin user:
    email: demodev@email.com
    pass: demodev@email.com

name: Bloom
domain: 127.0.0.1:8001
```

In settings_dev we have `FORCE_SITE_NAME=DemoDev`

# BUG: Authentication isn't working

In a shell:
    ```
    u = User.objects.get(email="demodev@email.com")
    u.check_password("demodev@email.com")
    ```
    This returns True as expected

## Attempt logging in with existing user:
Run `python manage.py runserver 8010`
Visit: http://127.0.0.1:8010/accounts/login/
    We are clearly looking at the DemoDev site as expected
Try to log in as demodev@email.com
    We get an error: "The email address and/or password you specified are not correct."
    This is a bug, we should be able to log in!

## Attempt signing up as demodev@email.com

Visit http://127.0.0.1:8010/accounts/signup/
Try to sign up as demodev@email.com
    We get an Integrity Error:
    ```
    duplicate key value violates unique constraint "freedom_ls_accounts_user_email_key"
DETAIL:  Key (email)=(demodev@email.com) already exists.
    ```

# BUG2: Ignoring FORCE_SITE_NAME when there is a site already set up

Run `python manage.py runserver 8001` then visit the home page then the Bloom site is active.

But FORCE_SITE_NAME is equal to DemoDev. So the DemoDev site should be active.
