# Frontend QA: miscellaneous small fixes

Eleven unrelated fixes made interactively, outside the SDD workflow. There is no spec and no plan —
`idea.md` is the whole brief — so this pass is the only gate the branch gets, and it has two jobs
rather than one:

1. **Confirm the eleven fixes landed**, and landed the way `idea.md` describes them.
2. **Confirm nothing else moved.** Several commits reached well past the item they were fixing.
   `get_cached_site()` learned to take `None`. `resolve_site_name()` was replaced by a new shared
   `site_display_name()`. `print.css` swapped ten theme-token fills for two new hex constants.
   `EMAIL_BACKEND` became a queueing backend in dev *and* prod. Two dev preview overrides were
   committed as `True`. Each of those is a wider blast radius than the one-line bug that prompted it.

§13 is the second job in one place: a read-only audit of everything that changed which no `idea.md`
item asked for.

**One failure is expected before you start.** See §3.6 — the form completion page is still missing
its Previous button. Record it as a failure; it is a real gap, not a stale check.

---

## 0. Setup

### 0.1 Build Tailwind first

`static/vendor/tailwind.output.css` is **not tracked in git**, and this branch adds `.accordion-*`,
`.flashcard-back*`, `.flashcard-flip-hint` and `.picture-figure-*` rules to
`tailwind.components.css`, plus four new tokens to the default theme.

```bash
npm run tailwind_build
```

**Skip this and every check in §5 and §6 fails for the wrong reason.** It is the highest-value step
in this section.

### 0.2 Dev server

```bash
PORT=$(.claude/ds/scripts/find_available_port.sh)
uv run python manage.py runserver $PORT
```

Base URL for everything below: `http://127.0.0.1:$PORT`

### 0.3 Mailpit

Development mail is captured by Mailpit (`dev_db/docker-compose.yaml`). Bring it up if it is not
already running, and keep the inbox open at `http://localhost:8025` — §1 and §2 read it.

### 0.4 Seed data

```bash
uv run python manage.py create_demo_data --yes
uv run python manage.py content_save demo_content DemoDev
uv run python manage.py qa_create_organisations DemoDev
uv run python manage.py qa_create_large_cohort DemoDev
uv run python manage.py qa_create_report_fixtures
```

`qa_create_large_cohort` gives §7 an organisation with enough learners to page through.
`qa_create_report_fixtures` gives §9 a cohort worth rendering a PDF for.

### 0.5 Log in

`http://127.0.0.1:$PORT/accounts/login/` — `demodev@email.com` / `demodev@email.com`.

### 0.6 What dev pins, and why some expected values look wrong

Two settings will make you file false bugs if you do not read this first.

**The tenant is called "FirstClass", not "DemoDev".** `config/settings_dev.py` sets
`FORCE_SITE_NAME = "DemoDev"` but also `HEADER_TITLE = "FirstClass"`, and the new shared
`site_display_name()` is HEADER_TITLE-first. So email subjects, the site header and the report cover
must all say **FirstClass**. "DemoDev" is the Site row's name and is the fallback, not the answer.

**Every course is visible and free.** `OVERRIDE_COURSE_VISIBILITY_TO_VISIBLE` and
`OVERRIDE_COURSE_ACCESS_TO_FREE` are now committed as `True` — that is one of the changes under test
(§10), not a mistake, and you must **not** revert them. Older QA plans in `spec_dd/3. done/` tell you
to; ignore them. The consequence for every other section: the dashboard and `/courses/` show more
courses than a real learner would, and no paywall stops you. Anything that looks like a visibility or
access regression is almost certainly this.

Courses are reached by title from `http://127.0.0.1:$PORT/courses/` rather than by slug below, since
slugs are derived at ingest.

---

## 1. Auth email names the tenant, not its domain

The subject prefix used to come from allauth's raw `Site.name` resolved off the HTTP host. On an
install whose Site row was never named, that prefix was a bare domain, and it disagreed with the
email body — which has always been HEADER_TITLE-first.

Trigger a password reset (`/accounts/password/reset/`) for `demodev@email.com`, and a fresh signup
verification for a new address. Read both in Mailpit.

| Check | Expected |
| --- | --- |
| 1.1 | The subject prefix is `[FirstClass]`. Not `[DemoDev]`, not `[127.0.0.1:$PORT]`, not a bare domain. |
| 1.2 | The prefix and the branded label in the body say the same thing. A disagreement between the two is the whole bug. |
| 1.3 | The logo and branding block still render. The adapter swapped `get_current_site` for `get_cached_site`, so this is where a wrong-Site resolution would show. |
| 1.4 | The verification / reset URL in the body is unbroken — no `=` line continuations, no wrap mid-URL. Click it; it must work end to end. |
| 1.5 | Both the plain-text and HTML parts carry the same unbroken URL. |

**1.6 — the fallback still works.** Blank `HEADER_TITLE` in `config/settings_dev.py`, trigger another
reset, and confirm the prefix becomes `[DemoDev]` — the Site row's name via `FORCE_SITE_NAME`. Restore
`HEADER_TITLE` afterwards.

---

## 2. Mail is queued onto the background worker

`freedom_ls/deployment/mail.py` is new. `EMAIL_BACKEND` now names a `QueuedEmailBackend` in dev and in
production; the worker rebuilds each message from a JSON payload and sends it through
`EMAIL_UPSTREAM_BACKEND`. Dev's task backend is `ImmediateBackend`, so the task runs inline and Mailpit
still receives mail within the request — what dev buys is that the serialise/rebuild round-trip is
exercised every session instead of running first in production.

| Check | Expected |
| --- | --- |
| 2.1 | `uv run python manage.py check` is clean. In particular no `freedom_ls_deployment.E007`. |
| 2.2 | A password reset returns the browser response promptly and the mail still lands in Mailpit. |
| 2.3 | In Mailpit's source view, `Content-Transfer-Encoding: 8bit` on both the text and the HTML part, and no `=` soft line breaks anywhere. The 8bit patch is destroyed by serialisation and has to be re-applied by the worker — this check is the only thing that catches it if it is not. |
| 2.4 | A message with non-ASCII in subject and body arrives with correct characters, not mojibake. |
| 2.5 | A task row exists for the send and reached SUCCEEDED. Django admin → Task results. |

**2.6 — E007 fires when you point the queue at itself.** In `config/settings_dev.py`, temporarily set
`EMAIL_UPSTREAM_BACKEND = "freedom_ls.deployment.mail.QueuedEmailBackend"` and run
`uv run python manage.py check`.

- The check errors with `freedom_ls_deployment.E007`.
- The message says the upstream backend *is* the queue and no mail would ever be delivered.
- The hint names `EMAIL_UPSTREAM_BACKEND` as the thing to change, not `EMAIL_BACKEND`.
- Revert the line when done.

**2.7 — the worker is now load-bearing, and its failure is silent.** This is the most important check
in this section, because a deployment that runs no worker now swallows every password reset and
verification email without an error anywhere.

1. In `config/settings_dev.py`, temporarily point `TASKS` at
   `django_tasks_db.DatabaseBackend` (the production backend) rather than the immediate one.
2. With **no** worker process running, trigger a password reset.
   **Expect:** the browser flow completes normally, the mail does **not** arrive in Mailpit, and an
   unpicked task row sits in the database. That is the silent failure.
3. Start `uv run python manage.py fls_run_worker`.
   **Expect:** the mail now arrives, with its URL still unbroken (2.3 again — this is the path where
   the 8bit patch has genuinely been through serialisation).
4. Revert `TASKS`.

**2.8 — attachments and alternatives survive the JSON payload.**
`uv run python manage.py sendtestemail --admins` or an equivalent send carrying both an HTML
alternative and a binary attachment. Both must arrive intact; the attachment must open.

**2.9 — the documented consequence.** Rendered messages, live reset links included, now sit as
plaintext rows in the task-results table until housekeeping prunes them. Confirm
`docs/product/security-and-data-handling.md` says so. This is a documented trade-off, not a bug —
record it as a note if the docs are missing it.

---

## 3. The form player gets the standard navigation footer

Course **Functionality Demo - show end with Quiz** — its items run topic, quiz, topic, quiz, so the
quiz at index 2 has something behind it and the quiz at index 4 is the last item.

Complete the first topic before opening the quiz, or sequential unlock will redirect you away from it.

| Check | Expected |
| --- | --- |
| 3.1 | The form **start** page has a **Previous** button on the left and the forward button on the right. |
| 3.2 | Put a topic page and this form start page side by side. The two footers match: same button height, same padding, same border above, same alignment. Before this branch the form's buttons were noticeably larger. |
| 3.3 | Previous navigates to the preceding topic. |
| 3.4 | Previous is a secondary button carrying a left chevron. The forward button is primary and carries a right chevron. |
| 3.5 | On the runner pages (`/form/…/page/N`), Previous and Next are the same smaller size and match each other. |
| 3.6 | The submit-confirm dialog still opens from the last runner page's Next. |

**3.7 — every forward state.** Five different actions render in that right-hand slot. Walk each and
check the label and icon:

| State | How to reach it | Icon |
| --- | --- | --- |
| Start | A form never opened | right chevron |
| Continue | A form with some pages filled, then navigate away and back | right chevron |
| Try again | A quiz failed against its pass mark | retry |
| Next | A completed form that is not the last item | right chevron |
| Finish course | The final quiz in the `end with Quiz` course | check |

**3.8 — the navigation is boosted, not a page load.** Previous sits inside `c-player-nav`, so clicking
it must swap `#interface-main` plus the out-of-band table of contents. Watch the network panel: a full
document request is a regression.

**3.9 — a form with nothing behind it.** A form that is a course's *first* item must show no Previous
button, and the forward button must stay right-aligned rather than sliding left. No demo course starts
with a form; `freedom_ls/learner_interface/tests/test_course_item_navigation.py` covers this in pytest.
Build the browser case with `fls-dev:qa-data-helper` if you want it, or record 3.9 as covered by the
suite.

**3.10 — the completion page. This is the expected failure.**

Finish a quiz to reach the completion page.

- The buttons there (Continue / Retry quiz / Return to course) are now the smaller size and centred.
  That part passes.
- **The expected behaviour is that the completion page carries back and forward buttons like every
  other page in the course player.** It does not — there is no Previous button. `idea.md` lists this
  as done, but only the button sizing was changed.

Record 3.10 as a **failure**. It is a real gap, deliberately left in this plan so it gets filed rather
than forgotten.

---

## 4. A required question's asterisk stays on the question's last line

The bug is `image.png` beside this file: the `*` wrapped onto a line of its own, and the question
number sat one line above the question text. The legend's flex layout was replaced with plain flow
plus an inlined `<p>`, and the asterisk is now glued to the last word with a non-breaking space.

Open any form page with a required question — the quiz in `Functionality Demo - show end with Quiz`.

| Check | Expected |
| --- | --- |
| 4.1 | The `*` sits immediately after the question's last word, on the same line. |
| 4.2 | The question number (`1.`) is on the same line as the question text, not one line above it. |
| 4.3 | Narrow the browser until the question wraps across two or three lines. The `*` still never lands alone on a new line. |
| 4.4 | Markdown inside a question — bold, links, inline code — still renders. |
| 4.5 | The `*` is `aria-hidden` and a screen-reader-only "(required)" is still present, so the requirement is announced exactly once. |
| 4.6 | A question that is not required shows no asterisk. |

**4.7 — the trade-off.** The legend now forces every direct `<p>` inline, so a question whose markdown
is more than one paragraph will run together on a single line. Find or author such a question and say
whether it reads acceptably. This is a judgement call, not a pass/fail — record what you see.

---

## 5. Content widgets

Course **Content Widgets - Demo Reference**. The flashcard and accordion live in topic
**4. interactive-widgets**; the picture widget in topic **2. media**.

Nothing here is visible unless §0.1 ran.

### Flashcard

| Check | Expected |
| --- | --- |
| 5.1 | The front face's corner label reads **Question** (was "Front"); the back reads **Answer** (was "Back"). |
| 5.2 | Both faces carry a "Tap to flip" hint with a retry icon, bottom-right. |
| 5.3 | The answer face is a subtle brand-tinted gradient panel, clearly distinguishable from the question face but not so strong that it fights the text. |
| 5.4 | Prose, bold text, links, inline code, tables and horizontal rules on the answer face are all legible against that tint — nothing keeps a colour meant for the plain surface. |
| 5.5 | Both faces are the same height and their content is vertically centred. |
| 5.6 | Flipping works by click, and by keyboard with Enter and Space. `aria-pressed` toggles. |
| 5.7 | The corner label and flip hint are not announced by a screen reader — the button's own label already carries the interaction. |
| 5.8 | Tab to the card: the focus ring is clearly visible. It now uses the theme's focus-ring colour rather than the primary colour. |

### Accordion

| Check | Expected |
| --- | --- |
| 5.9 | A closed accordion's summary is the normal text colour. |
| 5.10 | Hovering the summary tints its background, and the tint is clipped to the rounded corners rather than spilling into them. |
| 5.11 | Opening it shifts the summary text **and** the chevron to the brand primary colour, and rotates the chevron. |
| 5.12 | Keyboard focus draws a visible ring, inset so it is not clipped. |
| 5.13 | The summary row is visibly roomier than before, and the body's padding is unchanged. |
| 5.14 | A long title wraps without crushing the chevron, which stays pinned right. |

### Picture

| Check | Expected |
| --- | --- |
| 5.15 | The `description` now renders **on the page**, under the caption. It used to appear only inside the lightbox. |
| 5.16 | That description runs the full width of the card — it does not stop short where the button starts. |
| 5.17 | "Figure N" sits in its own gutter in a mono face, with no `:` after it. Colour and typeface do the separating now. |
| 5.18 | The trigger button reads **Expand** (was "Open image"). |
| 5.19 | With a title that wraps to several lines — the demo content has a deliberately long one — the title's first line sits level with the Expand button, and later lines hang indented under the first. |
| 5.20 | The lightbox still opens, shows the heading and the description beneath it, and closes both on Escape and on its close button. |
| 5.21 | A picture with no `description` renders no empty paragraph and no stray gap. |
| 5.22 | A picture with no `number` still shows "Figure:" in the lightbox heading. |

### Across themes and motion settings

**5.23 — run the flashcard and accordion checks again on the `first_class` theme.** The flashcard's
back gradient is mixed from `--color-primary`, so a different brand colour is the most likely way to
make the answer face illegible.

**5.24 — reduced motion.** With `prefers-reduced-motion: reduce`, the flashcard still flips and the
accordion still opens; only the animation is dropped, not the state change.

---

## 6. Course cards

`http://127.0.0.1:$PORT/courses/` and the learner dashboard.

| Check | Expected |
| --- | --- |
| 6.1 | The eyebrow chip on a card is sized to its own text. It must not stretch to the full card width. |
| 6.2 | Across a row of cards with different description lengths, every "Details" link sits at the same height, flush with the bottom of its card. |
| 6.3 | Both hold at mobile and tablet widths, where the cards stack. |

---

## 7. An organisation's cohorts and learners on its admin page

`http://127.0.0.1:$PORT/admin/freedom_ls_organisations/organisation/`. Use the organisation carrying
`qa_create_large_cohort`'s data for the pagination checks.

| Check | Expected |
| --- | --- |
| 7.1 | Opening an organisation shows a **Cohorts** tab listing only that organisation's cohorts, ordered by name, with a count and a change link per row. |
| 7.2 | Page through the Cohorts tab. No row repeats and none is skipped between pages. |
| 7.3 | A **Learners** tab lists learners read-only, ordered by email, with a count and a change link per row, and no add or delete controls. |
| 7.4 | Page through the Learners tab: every learner appears exactly once across all pages. |
| 7.5 | A **Related** row shows "Search this organisation's N learners". The link opens the Learner changelist filtered to that organisation, and the number of results matches N. |
| 7.6 | An organisation with no learners shows "No learners yet" as plain text, with no broken link. |
| 7.7 | With exactly one learner, the wording is singular. |
| 7.8 | Adding a cohort from the Cohorts tab works, and the new cohort belongs to the organisation's site. |

**7.9 — the add page.** Go to **Add organisation**. Neither tab appears, no Related row appears, and
submitting the form still creates an organisation. An inline left on the add page would make the form
refuse any submission that did not carry its management form, so a failure here looks like a create
that silently will not save.

**7.10 — the large organisation loads.** Open the organisation with many learners and compare its load
time against a small one. They should be comparable; a page that visibly stalls means the per-row user
lookup is not being prefetched.

---

## 8. Topic content preview in the admin

`http://127.0.0.1:$PORT/admin/freedom_ls_content_engine/topic/`.

| Check | Expected |
| --- | --- |
| 8.1 | A topic's change page shows a read-only **Content Preview** rendering its markdown as HTML — not escaped source, not raw markdown. |
| 8.2 | A topic with empty content shows an empty preview, not an error. |
| 8.3 | The **Add topic** page loads and saves normally. |
| 8.4 | For a topic whose content contains raw HTML, inspect the rendered preview: no `<script>` survives. The preview is sanitised. |

**8.5 — the risk this change introduces.** The preview renders with no request. Any content widget
that needs one will now 500 a page that previously never rendered markdown at all. Open the change
page of **every** seeded topic that uses `c-picture`, `c-flashcard`, `c-accordion`, an image grid, a
video embed, and a file reference. None may 500 or render visibly broken markup. This is the most
likely place in the branch for an unnoticed error.

**8.6 — a large topic.** Open the change page of the longest topic in the demo content. The whole
rendered page is now inlined into the admin form; confirm it still loads in reasonable time and the
form below it is still usable.

---

## 9. The cohort report prints on white paper

The report is a PDF, not a web page. The browser work is generating and downloading it; the checks are
made by opening the file.

Generate from `http://127.0.0.1:$PORT/admin/freedom_ls_reports/generatedreport/generate/` (Django admin
→ Reports → **Generate cohort report**), for one of
`qa_create_report_fixtures`' cohorts. Download it and open it.

| Check | Expected |
| --- | --- |
| 9.1 | Interior pages are white. Not the theme's tinted surface — that tint reading as a shaded panel laid over the sheet is the bug being fixed. |
| 9.2 | Table header rows, even-row banding, neutral chips, the cover card, the at-a-glance stat cells, the flags panel, the "no activity started" line and both ratio-bar tracks are all the same neutral grey. |
| 9.3 | Status tints (pass, fail, error) still win over the even-row banding where they overlap. |
| 9.4 | The cover names the tenant **FirstClass** (§0.6), and the organisation's logo or monogram still renders. |
| 9.5 | The cover page's colour band still bleeds to all three edges and is not clipped at the bottom. |

**9.6 — greyscale.** View or print the PDF in greyscale. Fills and text stay distinguishable, and a 0%
bar and a partial bar are both still visible on a banded row. The ratio bar is outlined rather than
merely filled precisely because the track and the band are the same grey on a greyscale print; if the
outline were lost, a 0% bar would vanish.

**9.7 — the other theme.** Generate again under the `first_class` theme. That theme's blue-grey
surface is what prompted this fix, so it is the one that proves it.

---

## 10. Dev shows every course as visible and free

Two lines in `config/settings_dev.py` flipped from `False` to `True`.

| Check | Expected |
| --- | --- |
| 10.1 | The `Content Widgets - Demo Reference` course is marked `coming_soon` in its content, yet it opens in the player rather than redirecting to its detail page. |
| 10.2 | A course whose access type is not free can be entered without any registration. |
| 10.3 | The badges on `/courses/` still report each course's **declared** visibility and access. The overrides change gating, not labelling — a badge that now claims a coming-soon course is published is a finding. |

**10.4 — the overrides are dev-only.** Grep confirms both settings appear in `config/settings_dev.py`
and nowhere in `config/settings_prod.py` or `config/settings_base.py`, and that both default to
`False` where they are declared. Also confirm `manage.py check --deploy` warns if a preview override
is on while `DEBUG` is off.

---

## 11. Deployment guards and the bootstrap command

Terminal work, no browser.

| Check | Expected |
| --- | --- |
| 11.1 | `uv run python manage.py setup_initial_prod_data someone@example.test` with no `--site-name` fails with a usage error and creates no Site. The option is now required. |
| 11.2 | The same command **with** `--site-name "QA Tenant"` creates the Site under that name, and that name is what a later email subject prefix uses for that install. |
| 11.3 | `.env.example` explains that mail is queued by default, that a deployment running no worker must set `EMAIL_BACKEND` back to the SMTP backend, and documents `EMAIL_TIMEOUT`. |
| 11.4 | `uv run python manage.py check --deploy` against the production settings raises nothing new beyond what `main` already raises. |

---

## 12. Regressions elsewhere

Every seam a commit widened past its own fix.

**12.1 — an email sent with no request in flight.** `format_email_subject` resolves the site from
allauth's request, and that request is `None` outside a request cycle. With `FORCE_SITE_NAME` set —
as dev has it — that is fine. Blank `FORCE_SITE_NAME`, then send an allauth email from
`uv run python manage.py shell`, and record what happens.

FLS sets no `SITE_ID`, so with neither pinned there is nothing left to resolve the site from and the
call can raise. If it does, that is a production hazard for any install that pins neither, and it
belongs in the report even though dev never meets it. Restore `FORCE_SITE_NAME` afterwards.

| Check | Expected |
| --- | --- |
| 12.2 | The site header still reads "FirstClass". It shares the same resolver the email subject and report cover now use. |
| 12.3 | The whole allauth loop still works: sign up, verify, log in, log out, reset password, log in again. |
| 12.4 | The other demo courses — `Course Parts`, `Standard Markdown`, `show end with Topic` — still play end to end, and the topic player footer is unchanged. |
| 12.5 | The other widgets on the widgets demo course — image grids, cards, callouts, tables, code blocks — are unaffected by the new component CSS. |
| 12.6 | Educator interface cohort and learner pages still load. The learner-management admin now imports from the organisations admin at module scope, so an import cycle would surface as a 500 somewhere unrelated. |
| 12.7 | The Django admin index loads, and every changelist this branch touched opens without error. |

**12.8 — mobile and tablet.** Repeat §3, §4, §5 and §6 at 375x812 and 768x1024. The form footer, the
wrapping asterisk, the picture caption row and the card grid are all layout-sensitive, and the form
runner's buttons keep their full-width mobile behaviour at the new smaller size — check that still
looks right rather than cramped.

---

## 13. What changed that nobody asked for

A read-only audit. Nothing here is a browser test; each item is either confirmed intentional or
written up under General notes in the report. None of it should be auto-fixed.

**13.1 — a whole spec folder was deleted.** `spec_dd/2. in progress/visual_polish/` (11 files) was
removed in the widget-polish commit, whose message says its items are covered there. Only one of its
three sub-ideas actually is:

| Deleted sub-idea | Covered? |
| --- | --- |
| `course-card-details-link-not-bottom-aligned` | **Yes** — §6.2 is exactly this fix. |
| `btn-sm-padding-crowds-the-button-icon` | **No.** Its proposed fix was a `.btn.btn-sm` padding change; no such rule changed. This branch adds three more `btn-sm` usages instead. |
| `side-panel-dialog-paints-a-full-column-focus-outline` | **No.** No `.side-panel-dialog` rule anywhere in the diff. |

Four research documents — roughly 565 lines, including a 308-line study of the theme override
boundary — went with it, deleted rather than moved to `spec_dd/3. done/`. Two diagnosed bugs and that
research are gone from the tree. **This is the clearest unexpected change on the branch.** It is a
process decision for a human, not a defect to fix here.

**13.2 — a hard-coded coupling.** `.picture-figure-title` carries a padding value calculated from
`btn-sm`'s current padding plus its border, in order to line the caption's first line up with the
Expand button. Changing `btn-sm` later — which 13.1's second deleted idea wanted — will silently break
§5.19. Record the coupling; do not fix it.

**13.3 — a contributed-inlines seam that overwrites.** The learner-management admin *assigns*
`OrganisationAdmin.inlines` rather than appending to it, so a second app contributing inlines would
silently replace these. The parallel summaries registry does append correctly. Note only.

**13.4 — a context-key ordering hazard.** The form view sets `previous_url` in its context before
spreading the player chrome context over it, so if the chrome ever returned that key it would win
silently. It does not today. Note only.

**13.5 — a docs gap.** `print.css` now owns two colours of its own rather than taking them from the
theme. A downstream project can no longer rebrand the report's paper colour through the theme's
surface tokens, and neither `docs/how tos/theme-fls.md` nor the report docs say what replaced them.
The flashcard's new tokens *were* documented; these were not.

**13.6 — the rest of the collateral.** Confirm each is intended:

- `.secrets.baseline` line-number churn.
- Doc updates across `docs/product/deployment.md`,
  `docs/product/security-and-data-handling.md`, `docs/deployment-security-checklist.md`,
  `docs/product/configuration-and-extension.md`, `docs/how tos/theme-fls.md`, and two plugin
  resource files. Read each against the behaviour it now describes.
- `demo_content/functionality_demo_content_widgets/2. media/content.md` gained a deliberately long
  picture title and a reworded description, to exercise §5.19. After running `content_save`,
  `git status --short demo_content/` must be clean.
- `EMAIL_TIMEOUT` is set in the production settings only, not dev. Mailpit is local, so this is
  probably deliberate — confirm it.

---

## What to report

For every check: pass, fail or blocked, with the URL, the account used, a screenshot of anything that
failed, and the `runserver` traceback if there is one.

Two things the report must say explicitly:

- Whether **§3.10** — the form completion page's missing Previous button — is the only failure, or
  whether others joined it. It is known-failing before the run starts.
- That the **§13** findings, the deleted `visual_polish` folder above all, are notes and human
  decisions rather than bugs to fix. They belong under General notes, not in the fix loop.
