# Implementation plan: conformance upgrade-notes & plugin-doc tie-ins (test-portability 4)

> **Layers 5 & 6 (Part-2 portion)**, split from the `fls-test-portability-part-2`
> plan. Cross-references to "Layer 3/4" point at the sibling
> `test_portability_*` slices.
>
> **Revised 2026-08-23.** Plugin paths corrected for `split-claude-plugin`
> (2026-07-28), and one out-of-slice task removed — see `1. spec.md`'s revision
> note. All three surviving tasks were re-verified as genuinely undone.

## Ground truth (confirmed against the current tree, 2026-08-23)

- `claude_plugins/fls-dev/commands/concrete/update_fls.md` runs the Part-1 marker
  selection (`-m "not playwright and not fls_internal and not ci_only"`) at four
  call sites — lines ~105, ~122, ~147, ~168 — and invokes **neither**
  `manage.py check` **nor** the conformance suite.
- `claude_plugins/fls-dev/commands/update_upgrade_notes.md` documents
  `requires_settings_change` and `changed_settings` (lines ~19-20, ~41, ~79) but
  draws **no** distinction between a hard/required settings change and an
  optional/informational one.
- `freedom_ls/contrib/conformance/` has shipped, so the `update_fls.md`
  instruction can name a real target.
- `claude_plugins/fls-dev/resources/template_repo_manifest.md`'s `urls.py`
  checklist is still out of sync with `config/urls.py` — it omits
  `applications/`, `interest/`, and the sitemap/robots wiring.

## Layer 5 — `update_upgrade_notes.md` guidance (D6)

Edit `claude_plugins/fls-dev/commands/update_upgrade_notes.md`:

- Add guidance to recognise a **hard/required** settings change (vs
  optional/informational). When a spec introduces a hard config requirement, set
  `requires_settings_change: true` with the specific keys in `changed_settings`.
- When the spec also adds a Layer-4 system check enforcing the requirement, say
  so in the notes, so `update_fls` can point the downstream at `manage.py check`.
- No new schema flag (D6): `requires_settings_change` + `changed_settings` is
  sufficient.

Note for the author: a changed **check ID** is also a hard settings change in
practice — a downstream's `SILENCED_SYSTEM_CHECKS` entry silently stops matching.
Layer 4's `freedom_ls_course_access.E001` → `.E002` re-ID is the worked example.

(This slice's own `upgrade_notes.md` is authored later in the SDD flow, not here.
It requires no downstream settings change; it will note the availability of the
conformance suite and of Layer 4's checks.)

## Layer 6 — plugin-doc touches (Part-2 portion)

- **`claude_plugins/fls-dev/commands/concrete/update_fls.md`:** add to the
  verification steps (a) invoke the Layer-3 conformance suite as the positive
  signal, and (b) run `uv run python manage.py check` so Layer-4 failures surface
  during an upgrade. Use the documented port pattern for any runserver step (do
  not hardcode 8000). Part 1 already switched the bare `pytest` call sites to the
  marker selection — leave those alone.
- **`claude_plugins/fls-dev/resources/template_repo_manifest.md`:** the actual
  checklist edit (add `applications/`, `interest/`, sitemap/robots wiring, and a
  `tests/` dir importing the conformance suite) is `/update_template_repo`'s job
  (SDD step 12) — **not implemented here**. This plan records the requirement so
  step 12 lands it; do **not** pre-empt that step by editing the manifest now.

## Task order

1. **Layer 5** — the `update_upgrade_notes.md` guidance.
2. **Layer 6** — the `update_fls.md` verification steps.
3. Confirm at SDD step 12 that `/update_template_repo` picks up the manifest
   requirement recorded above.

## Skills to use

- `sdd:claude-code-authoring` — for both slash-command edits
  (`commands/update_upgrade_notes.md`, `commands/concrete/update_fls.md`); its
  description explicitly covers writing/editing slash commands.
