  Templates use the x-collapse Alpine directive on expandable course parts (e.g.,
  in the TOC sidebar), but the @alpinejs/collapse plugin is not loaded on the
  page.

  As a result, when you expand/collapse a course part, the content
  appears/disappears instantly (snap open/closed) rather than with a smooth height
   animation. Alpine logs 6 warnings per page to the console:

  Alpine Warning: You can't use [x-collapse] without first installing the
  "collapse" plugin.

  The expand/collapse functionality still works because x-show handles the
  visibility toggle — x-collapse only adds the animated transition. So it's a
  polish/UX issue rather than a broken feature.


1. Validate that this bug exists
2. Fix it
