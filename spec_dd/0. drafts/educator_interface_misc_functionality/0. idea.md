Build the following into the educator interface:

# Educators should be able to Create Cohorts

- On the cohort list page (educator_interface/cohorts), add a "New" button
    - Clicking on the button should bring up a modal with a form
    - When the form is submitted:
        - if there are errors then display it on the form (test with duplicate names)

## Implementation

This needs to be implemented in a reusable way

default values:

- DataTablePanel.create_form = null
- CohortConfig.create_form = a form instance 

When rendering a DataTablePanel then check if there is a create_form.

# Educators should be able to Edit Cohorts 

On the educator interface, when you look at a specific cohort (educator_interface/cohorts/{cohort pk}) then it should be possible to 