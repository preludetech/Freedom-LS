Currently allauth just sends text emails. We need professional looking emails that are on-brand.

1. Create a base template for emails. 

- It should render well in all standard email readers.
- There should be a text fallback 
- it should make use of our brand tailwind styles, but should still be lightweight to render in a browser 
- Make use of overridable template partials for greetings so that any project based on FreedomLS can adjust how it says hello, and how it signs off 

2. Make sure all the AllAuth emails make use of the new template system

3. Document the email template system so it is clear what template context variables are available. This will make it easier for other projects to adjust the template behavior.