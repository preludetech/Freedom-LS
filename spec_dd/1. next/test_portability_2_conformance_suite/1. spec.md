# Spec: FLS conformance suite — `freedom_ls.contrib.conformance` (test-portability 2)

> **Layer 3**, split verbatim from the `fls-test-portability-part-2` spec.
> Everything below this note is copied unchanged from the parent spec; only this
> note and the selection of the relevant sections are new. The success-criteria
> block and decision letters keep the parent's numbering (so `#3` / `D3` / `D6`,
> which belong to the sibling slices, are intentionally absent here).

## Problem

FLS ships as a package installed into concrete downstream projects (e.g. First
Class). "Did I wire FLS up correctly?" is the signal a downstream actually needs
from a test run, and today it **isn't a supported, dedicated thing at all**. The
canonical failure is the First Class integration of the `home_page` update:

- `student_interface` newly required `settings.COURSE_ACCESS_BACKEND`, which
  First Class had never set. FLS's loader (`get_course_access_backend()`,
  `freedom_ls/course_access/loader.py:23-39`) is `@functools.cache`d and does
  `import_string(settings.COURSE_ACCESS_BACKEND)` with **no fallback** — an
  unset key raises only on the *first real request*, not at `manage.py check`
  time.
- First Class's root `urls.py` was missing the `applications/` include.

Both would 500 the catalogue/dashboard at runtime while `manage.py check` passed
clean. They were caught only as a side effect of FLS's own tests being vacuumed
up by the downstream's `pytest` — the exact coupling Part 1 removed. Removing the
noise leaves this real signal absent. Part 2 supplies it.

This is not hypothetical drift: `fls-claude-plugin/resources/template_repo_manifest.md`'s
own `urls.py` checklist (lines ~210-219) still omits `applications/`,
`interest/`, and the sitemap/robots wiring that `config/urls.py:51-73` actually
ships. A downstream following that checklist reproduces the opening bug verbatim.

## Goal

Give a concrete project a positive, executable **"am I wired correctly?"**
signal that catches the `COURSE_ACCESS_BACKEND` / missing-include class of bug
*before* runtime, at two altitudes:

- **Layer 3 — conformance suite:** an importable pytest module the downstream
  runs, exercising the real integration seams against *its own* settings.
- **Layer 4 — system checks:** `manage.py check` fails on the static config
  gaps, independent of any test run.

Plus the Layer 5 upgrade-notes tie-in and Layer 6 doc touches that keep the
convention alive.

### Success criteria

1. **Under FLS's own `config.settings_dev`** (all apps installed, nothing
   pruned), `pytest --collect-only -q freedom_ls/contrib/conformance` lists at
   minimum the node IDs enumerated in "Acceptance node IDs" below. This is the
   full-install baseline the suite *offers*, not a per-downstream mandate: a
   downstream that removes an app or prunes an internal route legitimately
   collects fewer (criterion #7, D9).
2. The conformance suite is **green under FLS's own `config.settings_dev`** (FLS
   is its own first consumer — `testpaths` already includes `freedom_ls`).
3. Deleting `COURSE_ACCESS_BACKEND` from settings makes `manage.py check` exit
   non-zero with `freedom_ls_student_interface.E001`; likewise overriding
   `AUTH_USER_MODEL` away from `freedom_ls_accounts.User` (with `accounts`
   installed) fails with `freedom_ls_accounts.E003`. Required-setting
   presence/value is enforced at `check` time, not by the conformance suite (D8).
4. With `course_applications` installed, removing its `applications/` include
   makes `test_fls_namespace_reverses[course_applications:apply]` fail
   specifically (one named failure, not a collection abort).
5. A downstream missing sitemap/robots wiring fails
   `test_reference_url_reverses[sitemap]` / `[robots_txt]`.
6. The suite passes under the existing `--disable-socket
   --allow-hosts=127.0.0.1,::1` config with no DB required.
7. **Override-friendly at two granularities:** (a) a downstream that removes an
   FLS app from `INSTALLED_APPS` (e.g. replaces `student_interface`) gets that
   app's probes **skipped**, not failed, and Layer-4 checks for that app do not
   fire; (b) a downstream that *keeps* an app but customises an **internal**-tier
   route can `conformance.drop(...)` it and that probe **skips**, while
   **contract**-tier routes (depended on by other FLS code) still hard-fail if
   they don't reverse (D9).

## Layer 3 — the `freedom_ls.contrib.conformance` suite

A new, **opt-in importable module** at `freedom_ls/contrib/conformance/`. A
downstream references it from its own `tests/` dir. It is deliberately **not** a
`pytest11` entry-point plugin: an entry point on `sys.path` would auto-activate
in *every* downstream unconditionally, re-creating the coupling Part 1 removed.
An importable module makes opt-in explicit and greppable — matching how DRF
(`rest_framework.test`), django-cms (`CMSTestCase`), and Wagtail
(`wagtail.test.utils`) ship reusable test helpers.

### Override-friendliness (primary design constraint)

FLS is meant to be **extended and overridden**. A concrete project may replace
or discard whole FLS apps — e.g. throw away `student_interface` entirely and
ship its own. The conformance suite must **verify only the seams a downstream
actually opted into, and never enforce rigidity on flexible surfaces.**

Mechanism, at **two granularities**:

- **Per app** — every probe is gated on the FLS app/feature it checks actually
  being installed, reusing Part 1's `INSTALLED_APPS`-keyed collection-safety
  pattern. When the relevant app is absent from `settings.INSTALLED_APPS`, its
  probes **skip** (not fail). A downstream that replaced `student_interface`
  simply doesn't run the `student_interface:*` probes — it gets a clean skip, not
  a wall of red.
- **Per route, within a kept app** — a downstream may keep an FLS app but
  customise routes inside it (e.g. keep `student_interface` but replace its
  `dashboard`). App-level gating alone would over-constrain this. So namespace
  probes are split into a **contract tier** (routes other FLS apps/backends
  reverse to — hard-required while the app is installed, because FLS itself
  breaks without them) and a prunable **internal tier** (app-only routes a
  downstream can `conformance.drop(...)` when it customises them). See D9 and the
  `FLS_NAMESPACE_PROBES` table.

The suite is therefore a signal for *"the FLS pieces I chose to use are wired
correctly"*, never *"you must use all of FLS, unchanged."*

Consequences baked into the design:

- Data tables are organised **per app** and within an app **per tier**
  (contract/internal — D9), importable in slices
  (`from freedom_ls.contrib.conformance.test_urls import *`), so a downstream can
  opt into a subset explicitly rather than gate-and-skip, and can `drop()`
  individual internal routes it has customised while keeping the app. (Probe
  submodules are `test_*`-named — see Packaging & placement for why.)
- Required-setting *presence/value* enforcement lives entirely in **Layer 4**
  (see D8), not here — a pure static settings question is a `manage.py check`
  job, and the same override-gating applies there: `COURSE_ACCESS_BACKEND` is
  only required when an app that consumes it (`student_interface` /
  `course_applications`) is installed; `AUTH_USER_MODEL`'s pinned value is
  checked only when `accounts` is installed. Layer 3 keeps only the
  *behavioural* backend probe (`test_configured_backend_instantiates`), which
  executes the loader end-to-end.
- The **only** deliberately-enforced baseline is sitemap/robots existence
  (Decision D1, a product requirement) — and even that is silenceable by a
  downstream that genuinely serves no public surface. Everything app-specific is
  skip-when-absent.
- Layer 4's checks are already app-conditional (E001/E002 fire only when
  `student_interface` is installed — see Layer 4), so the same
  override-friendliness holds at boot time.

### Opt-in surface

The package `__init__.py` re-exports every conformance test function under an
explicit `__all__`. Downstreams opt in with either form; the spec documents
both and recommends the second for collision safety:

```python
# tests/test_fls_conformance.py  (downstream) — simple form
from freedom_ls.contrib.conformance import *          # noqa: F401,F403

# ...or the collision-safe form (recommended in downstream docs)
from freedom_ls.contrib import conformance
test_fls_namespace_reverses = conformance.test_fls_namespace_reverses
# (etc.)
```

`import *` works because pytest collects any module-level callable whose *name*
matches `python_functions` (default `test_*`), regardless of where it was
defined; parametrize marks travel with the function object. The wildcard form is
zero-effort but silently shadows any same-named test the downstream already has,
and can leak the module's own imports into the downstream namespace — hence the
hard hygiene rules below.

### Hygiene rules (load-bearing — enforced in the module itself)

- **Every conformance submodule defines `__all__`** listing only its intended
  test functions, so `import *` is inert w.r.t. the module's own imports
  (`reverse`, `settings`, `apps`, …).
- **No non-test helper in the package is named `test_*`.** Private helpers use a
  `_`-prefix (`_resolve_backend_app()`); if a public non-test callable is ever
  needed, set `helper.__test__ = False`.

### What it verifies

Reads the downstream's **real** config (zero `override_settings` — it must
reflect actual runtime state) and calls FLS's **own production resolution code**
so it can't drift from runtime behaviour. All probes are driven from small,
explicitly-maintained data tables parametrized with
`pytest.mark.parametrize(..., ids=...)` so each seam surfaces as one
specifically-named failure, and "add a probe" is a documented step when a new
FLS app/setting lands.

**Data tables (module-level, the maintained surface):**

- `FLS_NAMESPACE_PROBES` — organised **per app namespace**, and within each app
  split into two tiers (see Decision D9), each entry a concrete `viewname` + any
  minimal kwargs needed to reverse:
  - **`contract`** — routes that FLS's *own* code reverses to from **another
    app** or from a **pluggable backend** (established by grepping the codebase,
    not guessed). Their absence breaks a piece of FLS the downstream *kept*, so
    they **hard-fail when their app is installed**. The current contract set:
    - `student_interface`: `course_home`, `initiate_course_access` (reversed by
      `course_access/backends.py`), `dashboard`, `course_detail` (reversed by
      `course_applications`);
    - `course_applications`: `apply`, `status`;
    - `course_interest`: `express_interest`;
    - `educator_interface`: `interface`;
    - `accounts`: `account_profile`, `legal_doc`.
  - **`internal`** — routes reversed **only** by their own app's templates
    (`student_interface`'s `courses`, `form_start`, `form_fill_page`,
    `course_finish`, …; `course_interest:remove_interest`;
    `accounts:complete_registration`). Probed by default (broad signal, D2), but
    a downstream that customises the app can **prune** them individually
    (`conformance.drop("student_interface:courses")`) without touching the
    contract tier. Pruned entries **skip**, they do not fail.
- `REFERENCE_URL_NAMES` — the **required** downstream-replicated wiring:
  `"sitemap"`, `"robots_txt"` (unnamespaced project-root names — see Decision D1).

**Probes** (each gated on its app being installed — skip when absent, per
"Override-friendliness"):

- `test_fls_namespace_reverses[<viewname>]` — each FLS route reverses, **only
  for apps the downstream has installed** (and, for `internal`-tier entries, only
  those the downstream has not pruned — D9). Would have caught the missing
  `applications/` include; skips cleanly for an app the downstream replaced, and
  for internal routes it customised while keeping the app. Contract-tier entries
  cannot be pruned: if their app is installed they must reverse, because other
  FLS code depends on them.
- `test_reference_url_reverses[<name>]` — `sitemap` and `robots_txt` reverse.
  These are **required** (see D1): a downstream must replicate FLS's reference
  sitemap/robots wiring; a `NoReverseMatch` here is a hard conformance failure.
- `test_configured_backend_instantiates` — calls FLS's own
  `get_course_access_backend()` and asserts it returns a backend (exercises
  `import_string` + instantiation end-to-end).
- `test_active_theme_resolves` — asserts `settings.RESOLVED_THEME_DIR` is an
  existing directory, or re-runs the **side-effect-free** `resolve_theme_dir(
  settings.FLS_THEME, settings.FLS_THEMES_DIRS)`. **Never call `configure_theme`**
  (it mutates `TEMPLATES`/`STATICFILES_DIRS` in place — see D4).
- `test_active_icon_set_resolves` — `settings.FREEDOM_LS_ICON_SET in ICON_SETS`
  and/or a real `render_icon()` of a semantic name (exercises
  `get_icon_backend()` + `build_svg()`).
- `test_migration_state_consistent` — `call_command("makemigrations",
  check=True, dry_run=True)`. This diffs models against migration files and
  needs **no DB connection**, so it stays green under `--disable-socket` with no
  `django_db` marker (see D5).

Tiny, fast, deterministic: stdlib + django + pytest only. No factories, demo
content, browser, or network.

### Packaging & placement

- `freedom_ls/contrib/conformance/` is auto-included by the existing
  `[tool.setuptools.packages.find] include = ["freedom_ls*"]` glob — **no
  `pyproject.toml` packaging change** needed to ship it.
- Because `testpaths` includes `freedom_ls`, FLS's own `uv run pytest` collects
  and runs these probes under `config.settings_dev`. This is desired (FLS
  asserts it conforms to its own suite) and is success criterion #2.
- **Probe-bearing submodules must be `test_*`-named** (e.g. `test_urls.py`,
  `test_settings.py`, `test_theme.py`) so they match FLS's `python_files =
  ["tests.py", "test_*.py", "*_tests.py"]`. A submodule named `urls.py` would
  **not** be collected by FLS's own run nor by criterion #1's `pytest
  --collect-only -q freedom_ls/contrib/conformance` — the probes would silently
  collect as zero and criterion #2 would pass vacuously. Per-app data tables live
  in the same `test_*` module as the probes that consume them, or in a plain
  sibling module imported by it. (Downstream collection is unaffected either way:
  the downstream's own `tests/test_fls_conformance.py` re-imports via `import *`
  and matches its own `python_files`.)
- **Ruff:** the existing `[tool.ruff.lint.per-file-ignores]` only relaxes paths
  containing a literal `tests/` segment, so these shipped-but-pytest-style files
  would otherwise trip `S101` (assert) etc. Add a per-file-ignore entry for
  `freedom_ls/contrib/conformance/**` (at minimum `S101`). They are pytest test
  functions even though they ship as production code.

## Key decisions

- **D1 — sitemap/robots are required downstream wiring, probed as a distinct
  category.** `sitemap`/`robots_txt` are *not* FLS-shipped namespaces; they live
  in the project-root `config/` (excluded from the package), which only
  *demonstrates* the wiring. Per product decision, a concrete project **must**
  have sitemaps and robots. So: the conformance suite includes **required**
  `test_reference_url_reverses[sitemap|robots_txt]` probes (hard failure if
  unwired), and the template-repo step ships the reference wiring by default.
  These probe unnamespaced names and are documented as "did you replicate FLS's
  reference wiring", distinct from the FLS-namespace probes.
- **D2 — probe all FLS app namespaces**, not just the idea's named subset:
  broadest wiring signal and matches the "add a probe when you add an app"
  discipline. Breadth is bounded by D7/D9: "all namespaces" means all *installed*
  ones, and within each, contract routes are required while internal routes are
  prunable.
- **D4 — theme probe calls `resolve_theme_dir` (idempotent), never
  `configure_theme`** (which mutates settings in place and would double-insert
  theme dirs).
- **D5 — migration probe uses `makemigrations --check --dry-run`** (no DB
  connection, socket-safe) rather than any form needing a live connection.
- **D7 — override-friendliness over completeness.** FLS is built to be extended
  and overridden; a downstream may discard whole apps. Probes and checks are
  gated on their app being installed and **skip/no-op when absent** — the suite
  never mandates using all of FLS. This constraint outranks D2's breadth: "probe
  all FLS app namespaces" means all *installed* ones. The sole enforced baseline
  is D1 (sitemap/robots), itself silenceable. Override-friendliness applies at
  **two granularities** — whole-app (gate on `INSTALLED_APPS`) and
  within-a-kept-app route (D9).
- **D8 — required-setting presence/value is Layer 4 only, not a Layer-3 probe.**
  "Is the setting set / does it equal its pinned value" is a pure static
  question, and `manage.py check` is built to answer it (`base/checks.py` already
  reads `settings` this way; `E001` already does it for `COURSE_ACCESS_BACKEND`).
  So there is **no `test_required_setting_present`** in the conformance suite;
  the required-settings table is not a Layer-3 maintained surface. Each check is
  registered from the app that consumes the setting (`COURSE_ACCESS_BACKEND` →
  `student_interface` E001/E002; `AUTH_USER_MODEL` pinned value → `accounts`
  E003), inheriting the same install-gating for free. Layer 3 keeps only the
  *behavioural* seam — `test_configured_backend_instantiates` executes the loader
  end-to-end, which a static check cannot. This avoids maintaining the same
  static assertion in two layers; it is deliberately not the `icons`
  "same-question-both-layers" echo, whose two sides ask materially different
  questions (see the Layer 3 vs Layer 4 division).
- **D9 — namespace probes split into contract vs internal tiers; internal routes
  are prunable.** App-level gating (D7) handles whole-app replacement but
  over-constrains a downstream that *keeps* an FLS app and customises a route
  inside it (FLS is built to be extended — "implement your own student interface
  without a dashboard"). A `NoReverseMatch` from a broken include and one from an
  intentional route removal are indistinguishable, so the only honest split is by
  **who depends on the route**: routes reversed by *another* FLS app or a
  pluggable backend are **contract** (their absence breaks a piece of FLS the
  downstream kept — hard-fail while the app is installed); routes reversed only by
  their own app's templates are **internal** (probed by default for D2 breadth,
  but `conformance.drop(...)`-able per route so customising them is a clean skip,
  not red). The contract set is established by grepping the codebase (§Data
  tables), not asserted by fiat, and grows via the documented "add a probe" step.
  Criterion #1's node IDs are the full-install baseline, never a per-downstream
  mandate.

## Acceptance node IDs

This is the **full-install baseline under FLS's own `config.settings_dev`**
(every app installed, nothing pruned) — the maximum the suite offers, *not* a set
every downstream must reproduce (a downstream that drops an app or prunes an
internal route collects a subset; see criterion #7 and D9). Under that baseline,
`pytest --collect-only -q freedom_ls/contrib/conformance` lists **at least** the
following. `[C]` marks contract-tier entries (hard-fail while their app is
installed); the rest are internal-tier (prunable when a downstream customises the
route):

```
test_fls_namespace_reverses[student_interface:dashboard]              # [C]
test_fls_namespace_reverses[student_interface:course_detail]          # [C]
test_fls_namespace_reverses[student_interface:course_home]            # [C]
test_fls_namespace_reverses[student_interface:initiate_course_access] # [C]
test_fls_namespace_reverses[student_interface:courses]
test_fls_namespace_reverses[course_applications:apply]                # [C]
test_fls_namespace_reverses[course_applications:status]               # [C]
test_fls_namespace_reverses[course_interest:express_interest]         # [C]
test_fls_namespace_reverses[educator_interface:interface]            # [C]
test_fls_namespace_reverses[accounts:account_profile]                 # [C]
test_fls_namespace_reverses[accounts:legal_doc]                       # [C]
test_reference_url_reverses[sitemap]
test_reference_url_reverses[robots_txt]
test_configured_backend_instantiates
test_active_theme_resolves
test_active_icon_set_resolves
test_migration_state_consistent
```

## Testing strategy

- **Conformance module:** FLS's own suite runs it under `config.settings_dev`
  (must be green). Add targeted negative tests (style: existing
  `course_access/tests/test_checks.py`) proving each probe *fails* when its seam
  is broken — e.g. an `@override_settings`-driven test asserting a bad URL table
  entry raises `NoReverseMatch`, and that `test_configured_backend_instantiates`
  raises when `COURSE_ACCESS_BACKEND` points at an unimportable path. These
  meta-tests are themselves `fls_internal`-safe (they use FLS settings) and prove
  the probes have teeth without a downstream.
- **Override-friendliness (criterion #7):** two meta-tests. (a) With an FLS app
  removed from `INSTALLED_APPS`, its probes report `skipped` (not `failed`) and
  its Layer-4 checks return `[]`. (b) With the app kept but an internal-tier route
  `drop()`-ed, that probe reports `skipped` while a contract-tier route with a
  broken include still `failed` — the executable proof the suite doesn't enforce
  rigidity on overridable surfaces yet still guards the integration contract.
