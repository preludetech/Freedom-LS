Currently, when we navigate around the educator interface then the whole page reloads. Rather use htmx to load only those parts of the page that are needed.

Make sure that the url bar works as it should:
- it should be updated upon navigation
- if we directly visit a url for a page or tab then it should work

The back and forward buttons should work.
