We need a fully functional and good looking educator panel.

# Left hand panel:

## Current

- the educator interface is making use of a switcher at the top
- there are a bunch of individual link/items under that
- when opening an instance then it shows up in the panel (eg viewing a specific cohort)

## Upgrade

- allow links to be grouped into sections with headings
- link to:
    - Table views
    - Instance Views (eg looking at the current organisation)
    - Base Views (just a collection of tabs/panels). This could be used for a dashboard

# Main area

We need to be able to navigate effectively through direct links, and through HTMX.

We should be able to represent the following in the url:
- which view
- which tab
- which panel
- which filter/sort/search on which table

# Right hand quick view

This needs to be implemented
