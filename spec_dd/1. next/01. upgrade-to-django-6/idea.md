Upgrade Django to Django 6

Do a full user interface QA test to make sure everything works as it should, make sure you check that emails are working correctly.

Things to pay attention to:

- Email addresses must be encoded correctly when allauth sends emails
- Django-template-partials are now a part of DJango
- Upgrade any other packages we need to along the way
- Go over all the files in .claude and update them. Make the minimal required changes

Also make suggestions about other things that should be built.
