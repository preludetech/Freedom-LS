Create a new claude skill/command for a full QA run of the whole system. It should understand what the student and educator experiences should be based on previous specs and
  output, then make a QA plan (or a bunch of QA plans) once the plans are in place run them. This might be multiple commands or a seperate workflow. It should then plug into a bug fixing workflow.
