Refer to the theming implementation. We need to make sure that the first_class theme tailwind configuration makes sense.

look at the design-system inside this spec folder and implement as first_class.

Do not add extra components or component classes that are not currently being used.
If you think a specific extra component class should be implemented because it will be useful later them mention it. It will need to then be implemented both in fist_class and in the base system.
