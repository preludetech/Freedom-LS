{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": ["@playwright/mcp@latest",
        "--output-dir", "${CLAUDE_PROJECT_DIR}/qa-screenshots",
        "--headless", "--isolated",
        "--image-responses", "omit",
        "--caps", "testing"]
    }
  }
}
